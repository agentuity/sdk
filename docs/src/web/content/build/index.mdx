---
title: Build
description: Build model calls, streaming routes, and background work in framework apps
---

import { Bot, Database, MessageSquareText, Rows3, Wrench } from 'lucide-react';

Add model calls, conversation state, tool use, and background work to a framework app that's already set up.

<Cards>
  <CardLink
    href="/build/agents"
    title="Build Agents"
    description="Build model-backed workflows with ordinary framework code"
    icon={<Bot />}
  />
  <CardLink
    href="/build/chat-and-streaming"
    title="Chat and Streaming"
    description="Stream model output and keep conversation state in a service client"
    icon={<MessageSquareText />}
  />
  <CardLink
    href="/build/tool-calling"
    title="Tool Calling"
    description="Let models call bounded app functions with typed inputs"
    icon={<Wrench />}
  />
  <CardLink
    href="/build/state-and-memory"
    title="State and Memory"
    description="Store app memory explicitly with KV, databases, and cookies"
    icon={<Database />}
  />
  <CardLink
    href="/build/background-work"
    title="Background Work"
    description="Move slow work out of request handlers with queues, KV, and durable streams"
    icon={<Rows3 />}
  />
</Cards>

## Pick a Starting Point

- Build a reusable model-backed workflow: [Build Agents](/build/agents)
- Return tokens as they arrive: [Chat and Streaming](/build/chat-and-streaming)
- Let a model call app functions: [Tool Calling](/build/tool-calling)
- Store memory outside the response: [State and Memory](/build/state-and-memory)
- Move slow work out of the request: [Background Work](/build/background-work)

The examples use direct service clients because that path works across all supported frameworks and scripts. Hono apps can also install `@agentuity/hono` and read the same clients from `c.var.*`.

<Callout type="info" title="Using a legacy runtime app?">
These pages treat "agent" as an application design pattern, not as `createAgent()`. If you're on the legacy runtime, use the [legacy docs](https://v2.agentuity.dev) instead.
</Callout>

## Related Docs

- [Frameworks](/frameworks): start from Next.js, Hono, TanStack Start, or another supported framework
- [Services](/services): choose the Agentuity client for storage, queues, tasks, schedules, sandbox, and observability
- [Deploy and Operate](/deploy-operate): run the same framework project with `agentuity dev` and `agentuity deploy`
