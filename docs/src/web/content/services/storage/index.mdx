---
title: Storage
description: Persistent storage clients for framework apps and deployed services
---

import { Key, Search, HardDrive, Activity } from 'lucide-react';

Choose storage by access pattern first: key lookup, semantic search, files, streams, or SQL. Start with the direct storage client or API, and use Hono `c.var.*` when you want the same managed clients injected into routes.

## Storage Types

<Cards>
  <CardLink
    href="/services/storage/key-value"
    title="Key-Value"
    description="Fast lookups, caching, configuration"
    icon={<Key />}
  />
  <CardLink
    href="/services/storage/vector"
    title="Vector"
    description="Semantic search, embeddings, RAG"
    icon={<Search />}
  />
  <CardLink
    href="/services/storage/object"
    title="Object (S3)"
    description="Files, images, documents, media"
    icon={<HardDrive />}
  />
  <CardLink
    href="/services/storage/durable-streams"
    title="Durable Streams"
    description="Large exports, audit logs, real-time data"
    icon={<Activity />}
  />
</Cards>

## When to Use Each Type

| Storage Type | Best For | Client Path |
|--------------|----------|-------------|
| [Key-Value](/services/storage/key-value) | Caching, session data, rate limits | `@agentuity/keyvalue` |
| [Vector](/services/storage/vector) | Semantic search, embeddings, RAG | `@agentuity/vector` |
| [Object](/services/storage/object) | Files, images, large blobs | Bun S3 APIs with Agentuity-provided credentials |
| [Durable Streams](/services/storage/durable-streams) | Logs, exports, event streams | `@agentuity/stream` |
| [Database](/services/database) | Relational data, complex queries | `@agentuity/postgres`, `@agentuity/drizzle`, or `@agentuity/db` |

Use storage services when data must outlive the current request, be shared across workers or routes, or be inspected and managed as an Agentuity resource.

## Access Patterns

| App Shape | KV | Vector | Database |
|-----------|----|--------|----------|
| Any server framework or script | `new KeyValueClient()` | `new VectorClient()` | `postgres()`, `createPostgresDrizzle()`, or `new DBClient(...)` |
| Hono with `@agentuity/hono` | `c.var.kv` | `c.var.vector` | use a direct database client |
| Browser code | Call your backend route | Call your backend route | Call your backend route |

<Callout type="info" title="Hono injects service clients">
`@agentuity/hono` creates the same dedicated clients used in standalone examples and stores them on `c.var.*`. Database clients are not injected by the Hono middleware; create them directly where your app initializes database access.
</Callout>

## Custom Storage

For service code that needs a different backend, you can implement [custom Key-Value, Vector, or Durable Stream storage](/services/storage/custom).
