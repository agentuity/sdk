{
	"name": "sdk-mono",
	"version": "0.0.48",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/**"
	],
	"scripts": {
		"build": "bun scripts/check-legacy-cli.ts && bunx tsc --build --force && bun run --filter='./apps/*' build",
		"test": "bun test && cd packages/cli && bun run test && cd ../../apps/testing && bun run test",
		"typecheck:apps": "bun run --filter='./apps/*' typecheck",
		"typecheck:packages": "bun run --filter='./packages/*' typecheck",
		"typecheck": "bun run typecheck:apps && bun run typecheck:packages",
		"clean": "rm -rf packages/*/dist apps/**/dist packages/*/*.tsbuildinfo",
		"format": "bunx prettier */** --write",
		"lint": "bunx eslint packages",
		"all": "bun clean && bun format && bun run build && bun lint && bun typecheck && bun run test",
		"whoami": "./packages/cli/bin/cli.ts auth whoami"
	},
	"devDependencies": {
		"@eslint/js": "^9.38.0",
		"@eslint/json": "^0.13.2",
		"@types/bun": "latest",
		"eslint": "^9.38.0",
		"eslint-plugin-react": "^7.37.5",
		"globals": "^16.4.0",
		"jiti": "^2.6.1",
		"prettier": "3.6.2",
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"typescript": "^5.9.0",
		"typescript-eslint": "^8.46.2"
	},
	"dependencies": {}
}
