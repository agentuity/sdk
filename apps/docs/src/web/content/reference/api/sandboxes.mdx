---
title: Sandboxes API
short_title: Sandboxes
description: Create and manage isolated execution environments with full lifecycle, file system, snapshot, and checkpoint support
---

{/* This file is auto-generated from Zod schemas. Do not edit manually. Run scripts/generate-api-reference.ts to regenerate. */}


<RegionPicker />

## Authentication

Most requests require a Bearer token. Pass your SDK key in the `Authorization` header. Public endpoints (such as listing and fetching public snapshots) are noted below and do not require authentication.

| Header | Value |
|--------|-------|
| `Authorization` | `Bearer YOUR_SDK_KEY` |

You can find your SDK key in the [Agentuity Console](https://app.agentuity.com) under your project settings.

---

## Sandbox Management

### Create Sandbox

Create a new sandbox execution environment.

<ApiEndpoint method="POST" path="/sandbox" />

#### Parameters

<ParamTable params={[
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Sandbox creation payload.

<ResponseFields fields={[
  {
    "name": "projectId",
    "type": "string",
    "description": "Project ID to associate the sandbox with",
    "required": false
  },
  {
    "name": "runtime",
    "type": "string",
    "description": "Runtime name (e.g., \"bun:1\", \"python:3.14\")",
    "required": false
  },
  {
    "name": "runtimeId",
    "type": "string",
    "description": "Runtime ID (e.g., \"srt_xxx\")",
    "required": false
  },
  {
    "name": "name",
    "type": "string",
    "description": "Optional sandbox name",
    "required": false
  },
  {
    "name": "description",
    "type": "string",
    "description": "Optional sandbox description",
    "required": false
  },
  {
    "name": "resources",
    "type": "object",
    "description": "Resource constraints for the sandbox",
    "required": false
  },
  {
    "name": "resources.memory",
    "type": "string",
    "description": "Memory limit (e.g., \"512Mi\", \"1Gi\")",
    "required": false
  },
  {
    "name": "resources.cpu",
    "type": "string",
    "description": "CPU limit (e.g., \"0.5\", \"1\")",
    "required": false
  },
  {
    "name": "resources.disk",
    "type": "string",
    "description": "Disk size limit (e.g., \"1Gi\", \"10Gi\")",
    "required": false
  },
  {
    "name": "env",
    "type": "object",
    "description": "Environment variables to set in the sandbox",
    "required": false
  },
  {
    "name": "network",
    "type": "object",
    "description": "Network configuration for the sandbox",
    "required": false
  },
  {
    "name": "network.enabled",
    "type": "boolean",
    "description": "Whether network access is enabled",
    "required": false
  },
  {
    "name": "network.port",
    "type": "number",
    "description": "Port to expose from the sandbox (1024-65535)",
    "required": false
  },
  {
    "name": "stream",
    "type": "object",
    "description": "Stream configuration for I/O redirection",
    "required": false
  },
  {
    "name": "stream.stdout",
    "type": "string",
    "description": "Stream ID for stdout output",
    "required": false
  },
  {
    "name": "stream.stderr",
    "type": "string",
    "description": "Stream ID for stderr output",
    "required": false
  },
  {
    "name": "stream.stdin",
    "type": "string",
    "description": "Stream ID for stdin input",
    "required": false
  },
  {
    "name": "stream.timestamps",
    "type": "boolean",
    "description": "Whether to include timestamps in output",
    "required": false
  },
  {
    "name": "timeout",
    "type": "object",
    "description": "Timeout settings for the sandbox",
    "required": false
  },
  {
    "name": "timeout.idle",
    "type": "string",
    "description": "Idle timeout duration (e.g., \"5m\", \"1h\")",
    "required": false
  },
  {
    "name": "timeout.execution",
    "type": "string",
    "description": "Maximum execution time (e.g., \"30m\", \"2h\")",
    "required": false
  },
  {
    "name": "command",
    "type": "object",
    "description": "Initial command to run in the sandbox",
    "required": false
  },
  {
    "name": "command.exec",
    "type": "string[]",
    "description": "Command and arguments to execute",
    "required": true
  },
  {
    "name": "command.files",
    "type": "object[]",
    "description": "Files to write before execution (deprecated: use top-level files)",
    "required": false
  },
  {
    "name": "command.files[].path",
    "type": "string",
    "description": "Path to the file relative to the sandbox workspace",
    "required": true
  },
  {
    "name": "command.files[].content",
    "type": "string",
    "description": "Base64-encoded file content",
    "required": true
  },
  {
    "name": "command.mode",
    "type": "string",
    "description": "Execution mode: oneshot runs once, interactive keeps running",
    "required": false
  },
  {
    "name": "files",
    "type": "object[]",
    "description": "Files to write to sandbox on creation",
    "required": false
  },
  {
    "name": "files[].path",
    "type": "string",
    "description": "Path to the file relative to the sandbox workspace",
    "required": true
  },
  {
    "name": "files[].content",
    "type": "string",
    "description": "Base64-encoded file content",
    "required": true
  },
  {
    "name": "snapshot",
    "type": "string",
    "description": "Snapshot ID to restore the sandbox from",
    "required": false
  },
  {
    "name": "dependencies",
    "type": "string[]",
    "description": "Apt packages to install when creating the sandbox",
    "required": false
  },
  {
    "name": "packages",
    "type": "string[]",
    "description": "npm/bun packages to install globally when creating the sandbox",
    "required": false
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "Optional user-defined metadata to associate with the sandbox",
    "required": false
  }
]} />

#### Response

Returns the sandbox ID, status, and optional stream URLs for stdout/stderr.

| Status | Description |
|--------|-------------|
| 201 | Sandbox created |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "sandboxId",
    "type": "string",
    "description": "Unique identifier for the created sandbox",
    "required": true
  },
  {
    "name": "status",
    "type": "string",
    "description": "Current status of the sandbox",
    "required": true
  },
  {
    "name": "url",
    "type": "string",
    "description": "Public URL for the sandbox (only set when a network port is configured)",
    "required": false
  },
  {
    "name": "stdoutStreamId",
    "type": "string",
    "description": "Stream ID for reading stdout",
    "required": false
  },
  {
    "name": "stdoutStreamUrl",
    "type": "string",
    "description": "URL for streaming stdout output",
    "required": false
  },
  {
    "name": "stderrStreamId",
    "type": "string",
    "description": "Stream ID for reading stderr",
    "required": false
  },
  {
    "name": "stderrStreamUrl",
    "type": "string",
    "description": "URL for streaming stderr output",
    "required": false
  },
  {
    "name": "auditStreamId",
    "type": "string",
    "description": "Stream ID for reading audit events",
    "required": false
  },
  {
    "name": "auditStreamUrl",
    "type": "string",
    "description": "URL for streaming audit events",
    "required": false
  }
]} />

#### Example

<ApiExample method="POST" path="/sandbox" body={{
  "name": "dev-sandbox",
  "runtime": "node-20",
  "resources": {
    "memory": 512
  },
  "env": {
    "NODE_ENV": "development"
  }
}} />

---

### List Sandboxes

List sandboxes with optional filtering and pagination.

<ApiEndpoint method="GET" path="/sandbox" />

#### Parameters

<ParamTable params={[
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "name",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by name"
  },
  {
    "name": "mode",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by mode"
  },
  {
    "name": "projectId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by project"
  },
  {
    "name": "status",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by status"
  },
  {
    "name": "live",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Only running sandboxes"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Pagination offset"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Field to sort by"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction"
  },
  {
    "name": "deletedOnly",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Only deleted sandboxes"
  }
]} />

#### Response

Returns paginated list of sandboxes.

| Status | Description |
|--------|-------------|
| 200 | Sandboxes returned |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "sandboxes",
    "type": "object[]",
    "description": "Array of sandbox information",
    "required": true
  },
  {
    "name": "sandboxes[].sandboxId",
    "type": "string",
    "description": "Unique sandbox identifier",
    "required": true
  },
  {
    "name": "sandboxes[].identifier",
    "type": "string",
    "description": "Short identifier used for DNS hostname",
    "required": false
  },
  {
    "name": "sandboxes[].name",
    "type": "string",
    "description": "Sandbox name",
    "required": false
  },
  {
    "name": "sandboxes[].description",
    "type": "string",
    "description": "Sandbox description",
    "required": false
  },
  {
    "name": "sandboxes[].status",
    "type": "string",
    "description": "Current status",
    "required": true
  },
  {
    "name": "sandboxes[].mode",
    "type": "string",
    "description": "Sandbox mode (interactive or oneshot)",
    "required": false
  },
  {
    "name": "sandboxes[].createdAt",
    "type": "string",
    "description": "Creation timestamp (ISO 8601)",
    "required": true
  },
  {
    "name": "sandboxes[].region",
    "type": "string",
    "description": "Region where the sandbox is running",
    "required": false
  },
  {
    "name": "sandboxes[].runtime",
    "type": "object",
    "description": "Runtime information",
    "required": false
  },
  {
    "name": "sandboxes[].runtime.id",
    "type": "string",
    "description": "Unique runtime identifier",
    "required": true
  },
  {
    "name": "sandboxes[].runtime.name",
    "type": "string",
    "description": "Runtime name (e.g., \"bun:1\", \"python:3.14\")",
    "required": true
  },
  {
    "name": "sandboxes[].runtime.iconUrl",
    "type": "string",
    "description": "URL for runtime icon",
    "required": false
  },
  {
    "name": "sandboxes[].runtime.brandColor",
    "type": "string",
    "description": "Brand color for the runtime (hex color code)",
    "required": false
  },
  {
    "name": "sandboxes[].runtime.tags",
    "type": "string[]",
    "description": "Optional tags for categorization",
    "required": false
  },
  {
    "name": "sandboxes[].snapshot",
    "type": "object",
    "description": "Snapshot information",
    "required": false
  },
  {
    "name": "sandboxes[].executions",
    "type": "number",
    "description": "Number of executions run in this sandbox",
    "required": true
  },
  {
    "name": "sandboxes[].exitCode",
    "type": "number",
    "description": "Exit code from the last execution (only available for terminated/failed sandboxes)",
    "required": false
  },
  {
    "name": "sandboxes[].stdoutStreamUrl",
    "type": "string",
    "description": "URL to the stdout output stream",
    "required": false
  },
  {
    "name": "sandboxes[].stderrStreamUrl",
    "type": "string",
    "description": "URL to the stderr output stream",
    "required": false
  },
  {
    "name": "sandboxes[].auditStreamId",
    "type": "string",
    "description": "ID of the audit event stream (eBPF/Tetragon security events)",
    "required": false
  },
  {
    "name": "sandboxes[].auditStreamUrl",
    "type": "string",
    "description": "URL to the audit event stream (eBPF/Tetragon security events)",
    "required": false
  },
  {
    "name": "sandboxes[].dependencies",
    "type": "string[]",
    "description": "Apt packages installed in the sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].packages",
    "type": "string[]",
    "description": "npm/bun packages installed globally in the sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].metadata",
    "type": "object",
    "description": "User-defined metadata associated with the sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].resources",
    "type": "object",
    "description": "Resource limits for this sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].resources.memory",
    "type": "string",
    "description": "Memory limit (e.g., \"500Mi\", \"1Gi\")",
    "required": false
  },
  {
    "name": "sandboxes[].resources.cpu",
    "type": "string",
    "description": "CPU limit in millicores (e.g., \"500m\", \"1000m\")",
    "required": false
  },
  {
    "name": "sandboxes[].resources.disk",
    "type": "string",
    "description": "Disk limit (e.g., \"500Mi\", \"1Gi\")",
    "required": false
  },
  {
    "name": "sandboxes[].cpuTimeMs",
    "type": "number",
    "description": "Total CPU time consumed in milliseconds (available when terminated)",
    "required": false
  },
  {
    "name": "sandboxes[].memoryByteSec",
    "type": "number",
    "description": "Total memory usage in byte-seconds (available when terminated)",
    "required": false
  },
  {
    "name": "sandboxes[].networkEgressBytes",
    "type": "number",
    "description": "Total network egress in bytes (available when terminated)",
    "required": false
  },
  {
    "name": "sandboxes[].networkEnabled",
    "type": "boolean",
    "description": "Whether network access is enabled for this sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].networkPort",
    "type": "number",
    "description": "Network port exposed from the sandbox (1024-65535)",
    "required": false
  },
  {
    "name": "sandboxes[].url",
    "type": "string",
    "description": "Public URL for the sandbox (only set if networkPort is configured)",
    "required": false
  },
  {
    "name": "sandboxes[].user",
    "type": "object",
    "description": "User who created the sandbox (if available)",
    "required": false
  },
  {
    "name": "sandboxes[].user.id",
    "type": "string",
    "description": "User ID",
    "required": true
  },
  {
    "name": "sandboxes[].user.firstName",
    "type": "string",
    "description": "User's first name",
    "required": false
  },
  {
    "name": "sandboxes[].user.lastName",
    "type": "string",
    "description": "User's last name",
    "required": false
  },
  {
    "name": "sandboxes[].agent",
    "type": "object",
    "description": "Agent associated with the sandbox (if available)",
    "required": false
  },
  {
    "name": "sandboxes[].agent.id",
    "type": "string",
    "description": "Agent ID",
    "required": true
  },
  {
    "name": "sandboxes[].agent.name",
    "type": "string",
    "description": "Agent name",
    "required": true
  },
  {
    "name": "sandboxes[].project",
    "type": "object",
    "description": "Project associated with the sandbox (if available)",
    "required": false
  },
  {
    "name": "sandboxes[].project.id",
    "type": "string",
    "description": "Project ID",
    "required": true
  },
  {
    "name": "sandboxes[].project.name",
    "type": "string",
    "description": "Project name",
    "required": true
  },
  {
    "name": "sandboxes[].org",
    "type": "object",
    "description": "Organization associated with the sandbox",
    "required": true
  },
  {
    "name": "sandboxes[].org.id",
    "type": "string",
    "description": "Organization ID",
    "required": true
  },
  {
    "name": "sandboxes[].org.name",
    "type": "string",
    "description": "Organization name",
    "required": true
  },
  {
    "name": "sandboxes[].timeout",
    "type": "object",
    "description": "Timeout configuration for this sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].timeout.idle",
    "type": "string",
    "description": "Idle timeout duration (e.g., \"10m0s\")",
    "required": false
  },
  {
    "name": "sandboxes[].timeout.execution",
    "type": "string",
    "description": "Execution timeout duration (e.g., \"5m0s\")",
    "required": false
  },
  {
    "name": "sandboxes[].command",
    "type": "object",
    "description": "Startup command configured for this sandbox",
    "required": false
  },
  {
    "name": "sandboxes[].command.exec",
    "type": "string[]",
    "description": "Command and arguments",
    "required": true
  },
  {
    "name": "sandboxes[].command.mode",
    "type": "string",
    "description": "Execution mode",
    "required": false
  },
  {
    "name": "total",
    "type": "number",
    "description": "Total count of sandboxes matching the filter",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox" />

---

### Get Sandbox

Retrieve a specific sandbox by ID.

<ApiEndpoint method="GET" path="/sandbox/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "includeDeleted",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Include deleted sandboxes"
  }
]} />

#### Response

Returns full sandbox details including resources, runtime, network, timeout, and usage metrics.

| Status | Description |
|--------|-------------|
| 200 | Sandbox returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="GET" path="/sandbox/sbx_abc123" />

---

### Destroy Sandbox

Destroy a sandbox and release all resources.

<ApiEndpoint method="DELETE" path="/sandbox/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Destroys the sandbox and releases all resources.

| Status | Description |
|--------|-------------|
| 200 | Sandbox destroyed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="DELETE" path="/sandbox/sbx_abc123" />

---

### Get Sandbox Status

Lightweight status check backed by Redis (~1ms). Optimized for polling.

<ApiEndpoint method="GET" path="/sandbox/status/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns the sandbox status.

| Status | Description |
|--------|-------------|
| 200 | Status returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "sandboxId",
    "type": "string",
    "description": "Unique identifier for the sandbox."
  },
  {
    "name": "status",
    "type": "string",
    "description": "Current status of the sandbox."
  },
  {
    "name": "exitCode",
    "type": "number",
    "description": "Exit code from the last execution, if terminated."
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox/status/sbx_abc123" />

---

### Pause Sandbox

Pause a running sandbox and create a checkpoint.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/pause" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Pauses the sandbox and creates a checkpoint.

| Status | Description |
|--------|-------------|
| 200 | Sandbox paused |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/pause" />

---

### Resume Sandbox

Resume a paused sandbox from its checkpoint.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/resume" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Resumes a paused sandbox from its checkpoint.

| Status | Description |
|--------|-------------|
| 200 | Sandbox resumed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/resume" />

---

### Update Environment

Update environment variables for a sandbox. Set a value to null to delete a variable.

<ApiEndpoint method="PATCH" path="/sandbox/env/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Environment variable updates.

<ResponseFields fields={[
  {
    "name": "env",
    "type": "object",
    "description": "Key-value pairs. Set value to null to delete a variable.",
    "required": true
  }
]} />

#### Response

Returns the current environment after update.

| Status | Description |
|--------|-------------|
| 200 | Environment updated |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "env",
    "type": "object",
    "description": "Current environment after update"
  }
]} />

#### Example

<ApiExample method="PATCH" path="/sandbox/env/sbx_abc123" body={{
  "env": {
    "NODE_ENV": "production",
    "OLD_VAR": null
  }
}} />

---

## Execution

### Execute Command

Execute a command in a sandbox.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/execute" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Command execution payload.

<ResponseFields fields={[
  {
    "name": "command",
    "type": "string[]",
    "description": "Command and arguments to execute",
    "required": true
  },
  {
    "name": "files",
    "type": "object[]",
    "description": "Files to write before execution",
    "required": false
  },
  {
    "name": "files[].path",
    "type": "string",
    "description": "File path relative to workspace",
    "required": true
  },
  {
    "name": "files[].content",
    "type": "string",
    "description": "Base64-encoded file content",
    "required": true
  },
  {
    "name": "timeout",
    "type": "string",
    "description": "Execution timeout (e.g., \"30s\", \"5m\")",
    "required": false
  },
  {
    "name": "stream",
    "type": "object",
    "description": "Stream configuration for output redirection",
    "required": false
  },
  {
    "name": "stream.stdout",
    "type": "string",
    "description": "Stream ID for stdout output",
    "required": false
  },
  {
    "name": "stream.stderr",
    "type": "string",
    "description": "Stream ID for stderr output",
    "required": false
  },
  {
    "name": "stream.timestamps",
    "type": "boolean",
    "description": "Whether to include timestamps in output",
    "required": false
  }
]} />

#### Response

Returns execution ID and stream URLs. If the sandbox was suspended, it is automatically resumed and the response includes `autoResumed: true`. Returns 409 if sandbox is busy.

| Status | Description |
|--------|-------------|
| 200 | Command executed (may include autoResumed: true if sandbox was suspended) |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |
| 409 | Conflict — sandbox is busy with another execution |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "executionId",
    "type": "string",
    "description": "Unique identifier for the execution",
    "required": true
  },
  {
    "name": "status",
    "type": "string",
    "description": "Current status of the execution",
    "required": true
  },
  {
    "name": "exitCode",
    "type": "number",
    "description": "Exit code of the executed command",
    "required": false
  },
  {
    "name": "durationMs",
    "type": "number",
    "description": "Execution duration in milliseconds",
    "required": false
  },
  {
    "name": "stdoutStreamUrl",
    "type": "string",
    "description": "URL for streaming stdout output",
    "required": false
  },
  {
    "name": "stderrStreamUrl",
    "type": "string",
    "description": "URL for streaming stderr output",
    "required": false
  },
  {
    "name": "outputTruncated",
    "type": "boolean",
    "description": "Whether the captured output was truncated due to size limits",
    "required": false
  }
]} />

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/execute" body={{
  "command": [
    "node",
    "-e",
    "console.log('hello')"
  ]
}} />

---

### Get Execution

Retrieve execution details. Use the `wait` parameter for long-polling.

<ApiEndpoint method="GET" path="/sandbox/execution/{executionId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "executionId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Execution ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "wait",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Long-poll duration (e.g., '60s', '5m'). Server holds connection until execution completes or timeout."
  }
]} />

#### Response

Returns execution details. Use the `wait` parameter for long-polling.

| Status | Description |
|--------|-------------|
| 200 | Execution returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Execution not found |

#### Example

<ApiExample method="GET" path="/sandbox/execution/exec_abc123" />

---

### List Executions

List executions for a specific sandbox.

<ApiEndpoint method="GET" path="/sandbox/sandboxes/{sandboxId}/executions" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return"
  }
]} />

#### Response

Returns list of executions for the sandbox.

| Status | Description |
|--------|-------------|
| 200 | Executions returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="GET" path="/sandbox/sandboxes/sbx_abc123/executions" />

---

## Events

### List Events

List lifecycle events for a specific sandbox. Events are returned in chronological order (oldest first) by default.

<ApiEndpoint method="GET" path="/sandbox/sandboxes/{sandboxId}/events" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return (default: 50, max: 100)"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction: 'asc' (default, oldest first) or 'desc' (newest first)"
  }
]} />

#### Response

Returns list of lifecycle events for the sandbox.

| Status | Description |
|--------|-------------|
| 200 | Events returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "events",
    "type": "object[]",
    "description": "List of sandbox events",
    "required": true
  },
  {
    "name": "events[].eventId",
    "type": "string",
    "description": "Unique identifier for the event",
    "required": true
  },
  {
    "name": "events[].sandboxId",
    "type": "string",
    "description": "ID of the sandbox this event belongs to",
    "required": true
  },
  {
    "name": "events[].type",
    "type": "string",
    "description": "Type of event (e.g., create, destroy, lifecycle:started)",
    "required": true
  },
  {
    "name": "events[].event",
    "type": "object",
    "description": "Event data payload",
    "required": true
  },
  {
    "name": "events[].createdAt",
    "type": "string",
    "description": "ISO timestamp when the event was recorded",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox/sandboxes/sbx_abc123/events" />

---

## File System

### Write Files

Write one or more files to the sandbox filesystem.

<ApiEndpoint method="POST" path="/fs/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Files to write.

<ResponseFields fields={[
  {
    "name": "files",
    "type": "object[]",
    "description": "Array of files to write",
    "required": true
  },
  {
    "name": "files[].path",
    "type": "string",
    "description": "Path to the file relative to the sandbox workspace",
    "required": true
  },
  {
    "name": "files[].content",
    "type": "string",
    "description": "Base64-encoded file content",
    "required": true
  }
]} />

#### Response

Returns the number of files written.

| Status | Description |
|--------|-------------|
| 200 | Files written |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "filesWritten",
    "type": "number",
    "description": "Number of files successfully written",
    "required": true
  }
]} />

#### Example

<ApiExample method="POST" path="/fs/sbx_abc123" body={{
  "files": [
    {
      "path": "/app/index.js",
      "content": "Y29uc29sZS5sb2coJ2hlbGxvJyk="
    }
  ]
}} />

---

### Read File

Read a file from the sandbox filesystem.

<ApiEndpoint method="GET" path="/fs/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "path",
    "type": "string",
    "in": "query",
    "required": true,
    "description": "File path to read"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns the raw file contents as a stream.

| Status | Description |
|--------|-------------|
| 200 | File contents returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox or file not found |

#### Example

<ApiExample method="GET" path="/fs/sbx_abc123?path=/home/user/file.txt" />

---

### Create Directory

Create a directory in the sandbox filesystem.

<ApiEndpoint method="POST" path="/fs/mkdir/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Directory creation payload.

<ResponseFields fields={[
  {
    "name": "path",
    "type": "string",
    "description": "Path to the directory to create",
    "required": true
  },
  {
    "name": "recursive",
    "type": "boolean",
    "description": "Create parent directories if needed",
    "required": false
  }
]} />

#### Response

Directory created successfully.

| Status | Description |
|--------|-------------|
| 200 | Directory created |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/fs/mkdir/sbx_abc123" body={{
  "path": "/app/src",
  "recursive": true
}} />

---

### Remove Directory

Remove a directory from the sandbox filesystem.

<ApiEndpoint method="POST" path="/fs/rmdir/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Directory removal payload.

<ResponseFields fields={[
  {
    "name": "path",
    "type": "string",
    "description": "Path to the directory to remove",
    "required": true
  },
  {
    "name": "recursive",
    "type": "boolean",
    "description": "Remove directory and all contents",
    "required": false
  }
]} />

#### Response

Directory removed successfully.

| Status | Description |
|--------|-------------|
| 200 | Directory removed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/fs/rmdir/sbx_abc123" body={{
  "path": "/app/tmp",
  "recursive": true
}} />

---

### Remove File

Remove a file from the sandbox filesystem.

<ApiEndpoint method="POST" path="/fs/rm/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

File removal payload.

<ResponseFields fields={[
  {
    "name": "path",
    "type": "string",
    "description": "Path to the file to remove",
    "required": true
  }
]} />

#### Response

File removed successfully.

| Status | Description |
|--------|-------------|
| 200 | File removed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/fs/rm/sbx_abc123" body={{
  "path": "/app/old-file.js"
}} />

---

### List Files

List files in a sandbox directory.

<ApiEndpoint method="GET" path="/fs/list/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "path",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Directory to list"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns list of files in the directory.

| Status | Description |
|--------|-------------|
| 200 | Files listed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "files",
    "type": "object[]",
    "description": "Array of file information",
    "required": true
  },
  {
    "name": "files[].path",
    "type": "string",
    "description": "File path relative to the listed directory",
    "required": true
  },
  {
    "name": "files[].size",
    "type": "number",
    "description": "File size in bytes",
    "required": true
  },
  {
    "name": "files[].isDir",
    "type": "boolean",
    "description": "Whether the entry is a directory",
    "required": true
  },
  {
    "name": "files[].isSymlink",
    "type": "boolean",
    "description": "Whether the entry is a symbolic link",
    "required": false
  },
  {
    "name": "files[].linkTarget",
    "type": "string",
    "description": "Target path of the symbolic link",
    "required": false
  },
  {
    "name": "files[].mode",
    "type": "string",
    "description": "Unix permissions as octal string (e.g., \"0644\")",
    "required": true
  },
  {
    "name": "files[].modTime",
    "type": "string",
    "description": "Modification time in RFC3339 format",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/fs/list/sbx_abc123" />

---

### Download Archive

Download a compressed archive of the sandbox filesystem.

<ApiEndpoint method="GET" path="/fs/download/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "path",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Directory to archive"
  },
  {
    "name": "format",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "'zip' or 'tar.gz'"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns a streaming archive of the sandbox filesystem.

| Status | Description |
|--------|-------------|
| 200 | Archive returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="GET" path="/fs/download/sbx_abc123" />

---

### Upload Archive

Upload and extract a compressed archive to the sandbox. Send raw binary with Content-Type: application/octet-stream.

<ApiEndpoint method="POST" path="/fs/upload/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "path",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Target directory"
  },
  {
    "name": "format",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Archive format"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Uploads and extracts a compressed archive to the sandbox. Send raw binary with Content-Type: application/octet-stream.

| Status | Description |
|--------|-------------|
| 200 | Archive uploaded and extracted |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/fs/upload/sbx_abc123" body="<binary archive data>" headers={{"Content-Type":"application/octet-stream"}} />

---

## Snapshots

### Create Snapshot

Create a snapshot of the sandbox filesystem.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/snapshot" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Snapshot creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Display name for the snapshot (letters, numbers, underscores, dashes only)",
    "required": false
  },
  {
    "name": "description",
    "type": "string",
    "description": "Description of the snapshot",
    "required": false
  },
  {
    "name": "tag",
    "type": "string",
    "description": "Tag for the snapshot (defaults to \"latest\")",
    "required": false
  },
  {
    "name": "public",
    "type": "boolean",
    "description": "Make the snapshot publicly accessible",
    "required": false
  }
]} />

#### Response

Returns the created snapshot.

| Status | Description |
|--------|-------------|
| 201 | Snapshot created |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/snapshot" body={{
  "name": "baseline",
  "tag": "v1.0"
}} />

---

### Get Snapshot

Retrieve a specific snapshot by ID.

<ApiEndpoint method="GET" path="/sandbox/snapshots/{snapshotId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns the snapshot object.

| Status | Description |
|--------|-------------|
| 200 | Snapshot returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Snapshot not found |

#### Example

<ApiExample method="GET" path="/sandbox/snapshots/snp_abc123" />

---

### List Snapshots

List snapshots with optional filtering and pagination.

<ApiEndpoint method="GET" path="/sandbox/snapshots" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by sandbox"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Pagination offset"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Field to sort by"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns paginated list of snapshots.

| Status | Description |
|--------|-------------|
| 200 | Snapshots returned |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "snapshots",
    "type": "object[]",
    "description": "List of snapshot entries",
    "required": true
  },
  {
    "name": "snapshots[].snapshotId",
    "type": "string",
    "description": "Unique identifier for the snapshot",
    "required": true
  },
  {
    "name": "snapshots[].runtimeId",
    "type": "string | null",
    "description": "Runtime ID associated with this snapshot",
    "required": false
  },
  {
    "name": "snapshots[].name",
    "type": "string",
    "description": "Display name for the snapshot",
    "required": true
  },
  {
    "name": "snapshots[].fullName",
    "type": "string",
    "description": "Full name with org slug for public snapshots (@slug/name:tag)",
    "required": false
  },
  {
    "name": "snapshots[].description",
    "type": "string | null",
    "description": "Description of the snapshot",
    "required": false
  },
  {
    "name": "snapshots[].message",
    "type": "string | null",
    "description": "Build message for the snapshot",
    "required": false
  },
  {
    "name": "snapshots[].tag",
    "type": "string | null",
    "description": "Tag for the snapshot (defaults to \"latest\")",
    "required": false
  },
  {
    "name": "snapshots[].sizeBytes",
    "type": "number",
    "description": "Total size of the snapshot in bytes",
    "required": true
  },
  {
    "name": "snapshots[].fileCount",
    "type": "number",
    "description": "Number of files in the snapshot",
    "required": true
  },
  {
    "name": "snapshots[].parentSnapshotId",
    "type": "string | null",
    "description": "ID of the parent snapshot (for incremental snapshots)",
    "required": false
  },
  {
    "name": "snapshots[].public",
    "type": "boolean",
    "description": "Whether the snapshot is publicly accessible",
    "required": false
  },
  {
    "name": "snapshots[].orgName",
    "type": "string",
    "description": "Organization name (for public snapshots)",
    "required": false
  },
  {
    "name": "snapshots[].orgSlug",
    "type": "string",
    "description": "Organization slug (for public snapshots)",
    "required": false
  },
  {
    "name": "snapshots[].org",
    "type": "object | null",
    "description": "Organization details (for public snapshots)",
    "required": false
  },
  {
    "name": "snapshots[].user",
    "type": "object | null",
    "description": "User who pushed the snapshot (for private snapshots)",
    "required": false
  },
  {
    "name": "snapshots[].createdAt",
    "type": "string",
    "description": "ISO timestamp when the snapshot was created",
    "required": true
  },
  {
    "name": "snapshots[].downloadUrl",
    "type": "string",
    "description": "URL to download the snapshot archive",
    "required": false
  },
  {
    "name": "snapshots[].files",
    "type": "object[] | null",
    "description": "List of files in the snapshot",
    "required": false
  },
  {
    "name": "snapshots[].userMetadata",
    "type": "object | null",
    "description": "User-defined metadata key-value pairs",
    "required": false
  },
  {
    "name": "total",
    "type": "number",
    "description": "Total number of snapshots matching the query",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox/snapshots" />

---

### Delete Snapshot

Delete a snapshot.

<ApiEndpoint method="DELETE" path="/sandbox/snapshots/{snapshotId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 204 | Snapshot deleted |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Snapshot not found |

#### Example

<ApiExample method="DELETE" path="/sandbox/snapshots/snp_abc123" />

---

### Update Snapshot Tag

Update the tag on a snapshot.

<ApiEndpoint method="PATCH" path="/sandbox/snapshots/{snapshotId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Tag update payload.

<ResponseFields fields={[
  {
    "name": "tag",
    "type": "string | null",
    "description": "New tag or null to remove tag",
    "required": true
  }
]} />

#### Response

Returns the updated snapshot.

| Status | Description |
|--------|-------------|
| 200 | Snapshot tag updated |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Snapshot not found |

#### Example

<ApiExample method="PATCH" path="/sandbox/snapshots/snp_abc123" body={{
  "tag": "v2.0"
}} />

---

### Get Snapshot Lineage

Get the ordered ancestry chain from a specified snapshot to root.

<ApiEndpoint method="GET" path="/sandbox/snapshots/lineage" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshot",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Snapshot ID or name:tag"
  },
  {
    "name": "name",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Snapshot name"
  },
  {
    "name": "tag",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Snapshot tag"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns ordered ancestry chain from specified snapshot to root.

| Status | Description |
|--------|-------------|
| 200 | Lineage returned |
| 401 | Unauthorized — invalid or missing API key |

#### Example

<ApiExample method="GET" path="/sandbox/snapshots/lineage" />

---

### Get Public Snapshot

Retrieve a public snapshot by ID, full name (@slug/name:tag), or name:tag. No authentication required.

<ApiEndpoint method="GET" path="/sandbox/snapshots/public/{snapshotRef}" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotRef",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID, full name (@slug/name:tag), or name:tag"
  }
]} />

#### Response

Returns public snapshot details. No authentication required.

| Status | Description |
|--------|-------------|
| 200 | Public snapshot returned |
| 404 | Snapshot not found |

#### Example

<ApiExample method="GET" path="/sandbox/snapshots/public/snp_abc123" />

---

### List Public Snapshots

List publicly available snapshots.

<ApiEndpoint method="GET" path="/sandbox/snapshots/public" />

#### Parameters

<ParamTable params={[
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Max 100"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Pagination offset"
  }
]} />

#### Response

Returns paginated list of public snapshots.

| Status | Description |
|--------|-------------|
| 200 | Public snapshots returned |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "snapshots",
    "type": "object[]",
    "description": "List of snapshot entries"
  },
  {
    "name": "snapshots[].snapshotId",
    "type": "string",
    "description": "Unique identifier for the snapshot"
  },
  {
    "name": "snapshots[].runtimeId",
    "type": "string | null",
    "description": "Runtime ID associated with this snapshot"
  },
  {
    "name": "snapshots[].name",
    "type": "string",
    "description": "Display name for the snapshot"
  },
  {
    "name": "snapshots[].fullName",
    "type": "string",
    "description": "Full name with org slug for public snapshots (@slug/name:tag)"
  },
  {
    "name": "snapshots[].description",
    "type": "string | null",
    "description": "Description of the snapshot"
  },
  {
    "name": "snapshots[].message",
    "type": "string | null",
    "description": "Build message for the snapshot"
  },
  {
    "name": "snapshots[].tag",
    "type": "string | null",
    "description": "Tag for the snapshot (defaults to \"latest\")"
  },
  {
    "name": "snapshots[].sizeBytes",
    "type": "number",
    "description": "Total size of the snapshot in bytes"
  },
  {
    "name": "snapshots[].fileCount",
    "type": "number",
    "description": "Number of files in the snapshot"
  },
  {
    "name": "snapshots[].parentSnapshotId",
    "type": "string | null",
    "description": "ID of the parent snapshot (for incremental snapshots)"
  },
  {
    "name": "snapshots[].public",
    "type": "boolean",
    "description": "Whether the snapshot is publicly accessible"
  },
  {
    "name": "snapshots[].orgName",
    "type": "string",
    "description": "Organization name (for public snapshots)"
  },
  {
    "name": "snapshots[].orgSlug",
    "type": "string",
    "description": "Organization slug (for public snapshots)"
  },
  {
    "name": "snapshots[].org",
    "type": "object | null",
    "description": "Organization details (for public snapshots)"
  },
  {
    "name": "snapshots[].user",
    "type": "object | null",
    "description": "User who pushed the snapshot (for private snapshots)"
  },
  {
    "name": "snapshots[].createdAt",
    "type": "string",
    "description": "ISO timestamp when the snapshot was created"
  },
  {
    "name": "snapshots[].downloadUrl",
    "type": "string",
    "description": "URL to download the snapshot archive"
  },
  {
    "name": "snapshots[].files",
    "type": "object[] | null",
    "description": "List of files in the snapshot"
  },
  {
    "name": "snapshots[].userMetadata",
    "type": "object | null",
    "description": "User-defined metadata key-value pairs"
  },
  {
    "name": "total",
    "type": "number",
    "description": "Total number of snapshots matching the query"
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox/snapshots/public" />

---

### Initialize Snapshot Build

Initialize a snapshot build. Returns a presigned upload URL for the snapshot archive.

<ApiEndpoint method="POST" path="/sandbox/snapshots/build" />

#### Parameters

<ParamTable params={[
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Snapshot build initialization payload.

<ResponseFields fields={[
  {
    "name": "runtime",
    "type": "string",
    "description": "Runtime identifier (name:tag or runtime ID)",
    "required": true
  },
  {
    "name": "name",
    "type": "string",
    "description": "Display name for the snapshot",
    "required": false
  },
  {
    "name": "tag",
    "type": "string",
    "description": "Tag for the snapshot",
    "required": false
  },
  {
    "name": "description",
    "type": "string",
    "description": "Description of the snapshot",
    "required": false
  },
  {
    "name": "contentHash",
    "type": "string",
    "description": "SHA-256 hash of snapshot content for change detection",
    "required": false
  },
  {
    "name": "force",
    "type": "boolean",
    "description": "Force rebuild even if content is unchanged",
    "required": false
  },
  {
    "name": "encrypt",
    "type": "boolean",
    "description": "Request encryption for the snapshot archive",
    "required": false
  },
  {
    "name": "public",
    "type": "boolean",
    "description": "Make snapshot public (enables virus scanning, disables encryption)",
    "required": false
  }
]} />

#### Response

Returns snapshot ID and presigned upload URL. If unchanged is true, content matches existing snapshot.

| Status | Description |
|--------|-------------|
| 200 | Build initialized |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "snapshotId",
    "type": "string",
    "description": "Unique identifier for the snapshot being built"
  },
  {
    "name": "uploadUrl",
    "type": "string",
    "description": "Pre-signed URL for uploading the snapshot archive (private snapshots only)"
  },
  {
    "name": "publicKey",
    "type": "string",
    "description": "PEM-encoded public key for encrypting the snapshot archive"
  },
  {
    "name": "unchanged",
    "type": "boolean",
    "description": "True if snapshot content is unchanged"
  },
  {
    "name": "existingId",
    "type": "string",
    "description": "ID of existing unchanged snapshot"
  },
  {
    "name": "existingName",
    "type": "string",
    "description": "Name of existing unchanged snapshot"
  },
  {
    "name": "existingTag",
    "type": "string",
    "description": "Tag of existing unchanged snapshot"
  }
]} />

#### Example

<ApiExample method="POST" path="/sandbox/snapshots/build" body={{
  "runtime": "node-20",
  "name": "my-app",
  "tag": "latest"
}} />

---

### Finalize Snapshot Build

Finalize a snapshot build after uploading the archive.

<ApiEndpoint method="POST" path="/sandbox/snapshots/{snapshotId}/finalize" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Snapshot finalization payload.

<ResponseFields fields={[
  {
    "name": "sizeBytes",
    "type": "number",
    "description": "Total size of the snapshot in bytes",
    "required": true
  },
  {
    "name": "fileCount",
    "type": "number",
    "description": "Number of files in the snapshot",
    "required": true
  },
  {
    "name": "files",
    "type": "object[]",
    "description": "Array of file metadata",
    "required": true
  },
  {
    "name": "files[].path",
    "type": "string",
    "description": "File path within the snapshot",
    "required": true
  },
  {
    "name": "files[].size",
    "type": "number",
    "description": "File size in bytes",
    "required": true
  },
  {
    "name": "files[].sha256",
    "type": "string",
    "description": "SHA256 hash of the file contents",
    "required": true
  },
  {
    "name": "files[].contentType",
    "type": "string",
    "description": "MIME type of the file",
    "required": true
  },
  {
    "name": "files[].mode",
    "type": "number",
    "description": "Unix file mode/permissions (e.g., 0o644)",
    "required": true
  },
  {
    "name": "dependencies",
    "type": "string[]",
    "description": "List of apt packages to install",
    "required": false
  },
  {
    "name": "packages",
    "type": "string[]",
    "description": "List of npm/bun packages to install globally",
    "required": false
  },
  {
    "name": "env",
    "type": "object",
    "description": "Environment variables to set",
    "required": false
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "User-defined metadata key-value pairs",
    "required": false
  }
]} />

#### Response

Returns the finalized snapshot.

| Status | Description |
|--------|-------------|
| 200 | Snapshot finalized |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Snapshot not found |

#### Example

<ApiExample method="POST" path="/sandbox/snapshots/snp_abc123/finalize" body={{
  "sizeBytes": 1048576,
  "fileCount": 42,
  "files": [
    {
      "path": "/app/index.js",
      "size": 1024
    }
  ]
}} />

---

### Upload Public Snapshot

Upload a gzip archive for public snapshots. Content-Type must be application/gzip. Includes virus scanning.

<ApiEndpoint method="PUT" path="/sandbox/snapshots/{snapshotId}/upload" />

#### Parameters

<ParamTable params={[
  {
    "name": "snapshotId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Snapshot ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Uploads a gzip archive for public snapshots. Content-Type must be application/gzip. Includes virus scanning.

| Status | Description |
|--------|-------------|
| 200 | Snapshot uploaded |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Snapshot not found |

#### Example

<ApiExample method="PUT" path="/sandbox/snapshots/snp_abc123/upload" body="<binary gzip data>" headers={{"Content-Type":"application/gzip"}} />

---

## Disk Checkpoints

### Create Checkpoint

Create a named checkpoint of the sandbox filesystem. Checkpoint names must be unique — creating a checkpoint with a name that already exists returns 409 Conflict. Disk checkpoints are persisted across pause/resume cycles.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/checkpoint" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Request Body

Checkpoint creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Name to assign to the new checkpoint",
    "required": true
  }
]} />

#### Response

Returns the created checkpoint.

| Status | Description |
|--------|-------------|
| 201 | Checkpoint created |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |
| 409 | Conflict — checkpoint name already exists, or sandbox is suspended |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "Globally unique checkpoint ID (ckpt_xxx)",
    "required": true
  },
  {
    "name": "name",
    "type": "string",
    "description": "User-provided checkpoint name",
    "required": true
  },
  {
    "name": "createdAt",
    "type": "string",
    "description": "ISO timestamp of creation",
    "required": true
  },
  {
    "name": "parent",
    "type": "string",
    "description": "Parent checkpoint name (empty for base)",
    "required": true
  }
]} />

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/checkpoint" body={{
  "name": "before-migration"
}} />

---

### List Checkpoints

List checkpoints for a specific sandbox. Disk checkpoints are persisted across pause/resume cycles.

<ApiEndpoint method="GET" path="/sandbox/checkpoints/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Returns list of checkpoints for the sandbox.

| Status | Description |
|--------|-------------|
| 200 | Checkpoints returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |
| 409 | Conflict — sandbox is suspended |

#### Example

<ApiExample method="GET" path="/sandbox/checkpoints/sbx_abc123" />

---

### Restore Checkpoint

Restore the sandbox filesystem to a checkpoint state.

<ApiEndpoint method="POST" path="/sandbox/{sandboxId}/checkpoint/{checkpointId}/restore" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "checkpointId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Checkpoint ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Restores the sandbox filesystem to the checkpoint state.

| Status | Description |
|--------|-------------|
| 200 | Checkpoint restored |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox or checkpoint not found |
| 409 | Conflict — sandbox is suspended |

#### Example

<ApiExample method="POST" path="/sandbox/sbx_abc123/checkpoint/ckpt_def456/restore" />

---

### Delete Checkpoint

Delete a checkpoint.

<ApiEndpoint method="DELETE" path="/sandbox/{sandboxId}/checkpoint/{checkpointId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  },
  {
    "name": "checkpointId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Checkpoint ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 204 | Checkpoint deleted |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox or checkpoint not found |
| 409 | Conflict — sandbox is suspended |

#### Example

<ApiExample method="DELETE" path="/sandbox/sbx_abc123/checkpoint/ckpt_def456" />

---

## Runtimes

### List Runtimes

List available sandbox runtimes with their requirements.

<ApiEndpoint method="GET" path="/sandbox/runtimes" />

#### Parameters

<ParamTable params={[
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Organization ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum results to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Pagination offset"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Field to sort by"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction"
  }
]} />

#### Response

Returns available sandbox runtimes with their requirements.

| Status | Description |
|--------|-------------|
| 200 | Runtimes returned |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "runtimes",
    "type": "object[]",
    "description": "Array of runtime information",
    "required": true
  },
  {
    "name": "runtimes[].id",
    "type": "string",
    "description": "Unique runtime identifier",
    "required": true
  },
  {
    "name": "runtimes[].name",
    "type": "string",
    "description": "Runtime name (e.g., \"bun:1\", \"python:3.14\")",
    "required": true
  },
  {
    "name": "runtimes[].description",
    "type": "string",
    "description": "Optional description",
    "required": false
  },
  {
    "name": "runtimes[].iconUrl",
    "type": "string",
    "description": "URL for runtime icon",
    "required": false
  },
  {
    "name": "runtimes[].brandColor",
    "type": "string",
    "description": "Brand color for the runtime (hex color code)",
    "required": false
  },
  {
    "name": "runtimes[].url",
    "type": "string",
    "description": "URL for runtime documentation or homepage",
    "required": false
  },
  {
    "name": "runtimes[].tags",
    "type": "string[]",
    "description": "Optional tags for categorization",
    "required": false
  },
  {
    "name": "runtimes[].requirements",
    "type": "object",
    "description": "Runtime requirements (memory, cpu, disk, network)",
    "required": false
  },
  {
    "name": "runtimes[].requirements.memory",
    "type": "string",
    "description": "Memory requirement (e.g., \"1Gi\")",
    "required": false
  },
  {
    "name": "runtimes[].requirements.cpu",
    "type": "string",
    "description": "CPU requirement (e.g., \"1\")",
    "required": false
  },
  {
    "name": "runtimes[].requirements.disk",
    "type": "string",
    "description": "Disk requirement (e.g., \"500Mi\")",
    "required": false
  },
  {
    "name": "runtimes[].requirements.networkEnabled",
    "type": "boolean",
    "description": "Whether network access is enabled",
    "required": true
  },
  {
    "name": "runtimes[].readme",
    "type": "string",
    "description": "Readme content in markdown format",
    "required": false
  },
  {
    "name": "total",
    "type": "number",
    "description": "Total count of runtimes",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/sandbox/runtimes" />

---

## CLI Endpoints

### List Sandboxes (Cross-Org)

List sandboxes across all organizations the user belongs to.

<ApiEndpoint method="GET" path="/cli/sandbox" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by name"
  },
  {
    "name": "mode",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "'oneshot' or 'interactive'"
  },
  {
    "name": "projectId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by project"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by organization"
  },
  {
    "name": "status",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by status"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Max 100"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Pagination offset"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Field to sort by"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction"
  }
]} />

#### Response

Lists sandboxes across all organizations the user belongs to.

| Status | Description |
|--------|-------------|
| 200 | Sandboxes returned |
| 401 | Unauthorized — invalid or missing API key |

#### Example

<ApiExample method="GET" path="/cli/sandbox" />

---

### Resolve Sandbox

Resolve a sandbox ID to its org, region, and project. Used for cross-org sandbox lookup.

<ApiEndpoint method="GET" path="/cli/sandbox/{sandboxId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "sandboxId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Sandbox ID"
  }
]} />

#### Response

Resolves a sandbox ID to its org, region, and project. Used for cross-org sandbox lookup.

| Status | Description |
|--------|-------------|
| 200 | Sandbox resolved |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Sandbox not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "Unique identifier for the sandbox.",
    "required": true
  },
  {
    "name": "name",
    "type": "string | null",
    "description": "Sandbox name, or null if unnamed.",
    "required": true
  },
  {
    "name": "region",
    "type": "string",
    "description": "Cloud region where the sandbox is running.",
    "required": true
  },
  {
    "name": "status",
    "type": "string",
    "description": "Current status of the sandbox.",
    "required": true
  },
  {
    "name": "orgId",
    "type": "string",
    "description": "Organization ID that owns the sandbox.",
    "required": true
  },
  {
    "name": "projectId",
    "type": "string | null",
    "description": "Project ID the sandbox belongs to, or null if unassigned.",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/cli/sandbox/sbx_abc123" />

---
