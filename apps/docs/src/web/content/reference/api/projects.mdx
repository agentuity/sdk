---
title: Projects API
description: Full project lifecycle management including deployments, agents, environment variables, and hostnames
---

Full project lifecycle management including deployments, agents, environment variables, and hostnames.

<RegionPicker />

## Authentication

All requests require a Bearer token. Pass your SDK key in the `Authorization` header.

| Header | Value |
|--------|-------|
| `Authorization` | `Bearer YOUR_SDK_KEY` |

You can find your SDK key in the [Agentuity Console](https://app.agentuity.com) under your project settings.

---

## Project Management

### Create Project

Create a new project within an organization.

<ApiEndpoint method="POST" path="/cli/project" />



#### Request Body

Project creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Project name (1-255 chars)",
    "required": true
  },
  {
    "name": "description",
    "type": "string",
    "description": "Project description",
    "required": false
  },
  {
    "name": "tags",
    "type": "array",
    "description": "Project tags",
    "required": false
  },
  {
    "name": "orgId",
    "type": "string",
    "description": "Organization ID",
    "required": true
  },
  {
    "name": "cloudRegion",
    "type": "string",
    "description": "Cloud region code",
    "required": true
  },
  {
    "name": "domains",
    "type": "array",
    "description": "Custom domains",
    "required": false
  }
]} />

#### Response

Returns the created project ID and SDK key.

| Status | Description |
|--------|-------------|
| 201 | Project created |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "Project ID"
  },
  {
    "name": "sdkKey",
    "type": "string",
    "description": "SDK key for the new project"
  }
]} />

#### Example

<ApiExample method="POST" path="/cli/project" body={{
  "name": "my-project",
  "orgId": "org_abc123",
  "cloudRegion": "usw"
}} />

---

### List Projects

List all projects accessible to the authenticated user.

<ApiEndpoint method="GET" path="/cli/project" />

#### Parameters

<ParamTable params={[
  {
    "name": "hasDeployment",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Filter to projects with deployments"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Max results (default 1000, max 10000)"
  }
]} />

#### Response

Returns a list of projects.

| Status | Description |
|--------|-------------|
| 200 | Projects returned |
| 401 | Unauthorized — invalid or missing API key |

#### Example

<ApiExample method="GET" path="/cli/project" />

---

### Get Project

Retrieve a specific project by ID. The `mask` and `includeProjectKeys` query parameters both default to `true` when omitted.

<ApiEndpoint method="GET" path="/cli/project/{id}" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "mask",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Mask secrets (default true)"
  },
  {
    "name": "includeProjectKeys",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Include SDK keys (default true)"
  }
]} />

#### Response

Returns the project details.

| Status | Description |
|--------|-------------|
| 200 | Project returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="GET" path="/cli/project/proj_abc123" />

---

### Delete Projects

Delete one or more projects by ID.

<ApiEndpoint method="DELETE" path="/cli/project" />



#### Request Body

Project deletion payload.

<ResponseFields fields={[
  {
    "name": "ids",
    "type": "array",
    "description": "Array of project IDs to delete",
    "required": true
  }
]} />

#### Response

Returns array of deleted project IDs.

| Status | Description |
|--------|-------------|
| 200 | Projects deleted |
| 401 | Unauthorized — invalid or missing API key |

#### Example

<ApiExample method="DELETE" path="/cli/project" body={{
  "ids": [
    "proj_abc123"
  ]
}} />

---

### Check Project Exists

Check if a project with the given name already exists.

<ApiEndpoint method="GET" path="/cli/project/exists/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project name (URL-encoded)"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": true,
    "description": "Organization ID"
  }
]} />

#### Response

Returns true (HTTP 409) if exists, false (HTTP 422) if not.

| Status | Description |
|--------|-------------|
| 409 | Project exists |
| 422 | Project does not exist |
| 401 | Unauthorized — invalid or missing API key |

#### Example

<ApiExample method="GET" path="/cli/project/exists/my-project?orgId=org_abc123" />

---

### Update Project Region

Update the cloud region for a project.

<ApiEndpoint method="PATCH" path="/cli/project/{id}" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Request Body

Region update payload.

<ResponseFields fields={[
  {
    "name": "cloudRegion",
    "type": "string",
    "description": "New cloud region",
    "required": true
  }
]} />

#### Response

Returns the updated project.

| Status | Description |
|--------|-------------|
| 200 | Project updated |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="PATCH" path="/cli/project/proj_abc123" body={{
  "cloudRegion": "use"
}} />

---

## Environment Variables

### Update Env Variables

Update environment variables and secrets for a project.

<ApiEndpoint method="PUT" path="/cli/project/{id}/env" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Request Body

Environment variable update payload.

<ResponseFields fields={[
  {
    "name": "env",
    "type": "object",
    "description": "Environment variables",
    "required": false
  },
  {
    "name": "secrets",
    "type": "object",
    "description": "Secret values",
    "required": false
  }
]} />

#### Response

Returns the updated environment variables.

| Status | Description |
|--------|-------------|
| 200 | Environment variables updated |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="PUT" path="/cli/project/proj_abc123/env" body={{
  "env": {
    "DB_HOST": "localhost"
  },
  "secrets": {
    "DB_PASS": "secret"
  }
}} />

---

### Delete Env Variables

Delete specific environment variables and secrets from a project.

<ApiEndpoint method="DELETE" path="/cli/project/{id}/env" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Request Body

Environment variable deletion payload.

<ResponseFields fields={[
  {
    "name": "env",
    "type": "array",
    "description": "Env var keys to delete",
    "required": false
  },
  {
    "name": "secrets",
    "type": "array",
    "description": "Secret keys to delete",
    "required": false
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 204 | Environment variables deleted |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="DELETE" path="/cli/project/proj_abc123/env" body={{
  "env": [
    "OLD_VAR"
  ],
  "secrets": [
    "OLD_SECRET"
  ]
}} />

---

## Agents

### List Agents

List agents for a project.

<ApiEndpoint method="GET" path="/cli/agent/{projectId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by deployment ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by organization ID"
  }
]} />

#### Response

Returns a list of agents for the project.

| Status | Description |
|--------|-------------|
| 200 | Agents returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "Agent ID"
  },
  {
    "name": "name",
    "type": "string",
    "description": "Agent name"
  },
  {
    "name": "description",
    "type": "string",
    "description": "Agent description"
  },
  {
    "name": "identifier",
    "type": "string",
    "description": "Agent identifier"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "description": "Deployment ID"
  },
  {
    "name": "devmode",
    "type": "boolean",
    "description": "Whether devmode is active"
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "Agent metadata"
  },
  {
    "name": "createdAt",
    "type": "string",
    "description": "Creation timestamp"
  },
  {
    "name": "updatedAt",
    "type": "string",
    "description": "Last update timestamp"
  },
  {
    "name": "evals",
    "type": "array",
    "description": "Associated evaluations"
  }
]} />

#### Example

<ApiExample method="GET" path="/cli/agent/proj_abc123" />

---

### Get Agent by Identifier

Retrieve a specific agent by its identifier.

<ApiEndpoint method="GET" path="/cli/agent/{projectId}?identifier={identifier}" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "identifier",
    "type": "string",
    "in": "query",
    "required": true,
    "description": "Agent identifier"
  }
]} />

#### Response

Returns the agent matching the identifier.

| Status | Description |
|--------|-------------|
| 200 | Agent returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Agent not found |

#### Example

<ApiExample method="GET" path="/cli/agent/proj_abc123?identifier=my-agent" />

---

## Deployments

### List Deployments

List deployments for a project.

<ApiEndpoint method="GET" path="/cli/project/{projectId}/deployments" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Max results (default 10)"
  },
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by organization ID"
  }
]} />

#### Response

Returns a list of deployments for the project.

| Status | Description |
|--------|-------------|
| 200 | Deployments returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="GET" path="/cli/project/proj_abc123/deployments" />

---

### Get Deployment

Retrieve a specific deployment by ID.

<ApiEndpoint method="GET" path="/cli/project/{projectId}/deployments/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Returns the deployment details.

| Status | Description |
|--------|-------------|
| 200 | Deployment returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="GET" path="/cli/project/proj_abc123/deployments/dep_def456" />

---

### Delete Deployment

Delete a specific deployment.

<ApiEndpoint method="DELETE" path="/cli/project/{projectId}/deployments/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Returns confirmation of deletion.

| Status | Description |
|--------|-------------|
| 200 | Deployment deleted |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="DELETE" path="/cli/project/proj_abc123/deployments/dep_def456" />

---

### Rollback Deployment

Rollback to a specific deployment version.

<ApiEndpoint method="POST" path="/cli/project/{projectId}/deployments/{deploymentId}/rollback" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Rolls back to the specified deployment version.

| Status | Description |
|--------|-------------|
| 200 | Rollback initiated |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="POST" path="/cli/project/proj_abc123/deployments/dep_def456/rollback" />

---

### Undeploy Project

Undeploy all active deployments for a project.

<ApiEndpoint method="POST" path="/cli/project/{projectId}/deployments/undeploy" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Response

Returns confirmation of undeployment.

| Status | Description |
|--------|-------------|
| 200 | Project undeployed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Example

<ApiExample method="POST" path="/cli/project/proj_abc123/deployments/undeploy" />

---

### Get Deployment Logs

Retrieve logs for a specific deployment.

<ApiEndpoint method="GET" path="/cli/project/{projectId}/deployments/{deploymentId}/logs" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  },
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Max results (default 100)"
  }
]} />

#### Response

Returns deployment log entries.

| Status | Description |
|--------|-------------|
| 200 | Logs returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="GET" path="/cli/project/proj_abc123/deployments/dep_def456/logs" />

---

### Get Deployment Info

Lightweight deployment lookup — returns ID, project, org, region, state, and active status.

<ApiEndpoint method="GET" path="/cli/deployment/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Lightweight deployment lookup — returns ID, project, org, region, state, and active status.

| Status | Description |
|--------|-------------|
| 200 | Deployment info returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="GET" path="/cli/deployment/dep_abc123" />

---

## Deploy Pipeline

### Start Deployment

Start a new deployment for a project.

<ApiEndpoint method="POST" path="/cli/deploy/2/start/{projectId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "projectId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Request Body

Deployment configuration payload.

<ResponseFields fields={[
  {
    "name": "resources",
    "type": "object",
    "description": "{ memory?, cpu?, disk? }",
    "required": false
  },
  {
    "name": "mode",
    "type": "object",
    "description": "{ type: 'on-demand'|'provisioned', idle? }",
    "required": false
  },
  {
    "name": "dependencies",
    "type": "array",
    "description": "Deployment dependencies",
    "required": false
  },
  {
    "name": "domains",
    "type": "array",
    "description": "Custom domains",
    "required": false
  }
]} />

#### Response

Returns deployment details and upload information.

| Status | Description |
|--------|-------------|
| 201 | Deployment started |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "Deployment ID"
  },
  {
    "name": "orgId",
    "type": "string",
    "description": "Organization ID"
  },
  {
    "name": "publicKey",
    "type": "string",
    "description": "For encrypting the deployment archive"
  },
  {
    "name": "buildLogsStreamURL",
    "type": "string",
    "description": "Pulse stream URL for build logs"
  }
]} />

#### Example

<ApiExample method="POST" path="/cli/deploy/2/start/proj_abc123" body={{
  "resources": {
    "memory": 512,
    "cpu": 1
  }
}} />

---

### Upload Build Metadata

Upload build metadata for a deployment in progress.

<ApiEndpoint method="PUT" path="/cli/deploy/2/start/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Returns pre-signed upload URLs for the deployment archive and assets.

| Status | Description |
|--------|-------------|
| 200 | Upload URLs returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="PUT" path="/cli/deploy/2/start/dep_abc123" />

---

### Complete Deployment

Signal that a deployment upload is complete and ready for activation.

<ApiEndpoint method="POST" path="/cli/deploy/2/complete/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Returns warmup stream and public URL information.

| Status | Description |
|--------|-------------|
| 200 | Deployment completed |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "streamId",
    "type": "string",
    "description": "Warmup logs stream"
  },
  {
    "name": "publicUrls",
    "type": "object",
    "description": "{ latest, deployment, custom[], vanityDeployment?, vanityProject? }"
  }
]} />

#### Example

<ApiExample method="POST" path="/cli/deploy/2/complete/dep_abc123" />

---

### Get Deploy Status

Check the current status of a deployment.

<ApiEndpoint method="GET" path="/cli/deploy/2/status/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Response

Returns the current deployment state.

| Status | Description |
|--------|-------------|
| 200 | Status returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "state",
    "type": "string",
    "description": "\\`pending\\`, \\`building\\`, \\`deploying\\`, \\`failed\\`, or \\`completed\\`"
  }
]} />

#### Example

<ApiExample method="GET" path="/cli/deploy/2/status/dep_abc123" />

---

### Report Deploy Failure

Report a deployment failure with error details.

<ApiEndpoint method="POST" path="/cli/deploy/2/fail/{deploymentId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID"
  }
]} />

#### Request Body

Failure report payload.

<ResponseFields fields={[
  {
    "name": "error",
    "type": "string",
    "description": "Error message",
    "required": false
  },
  {
    "name": "diagnostics",
    "type": "object",
    "description": "Diagnostic information",
    "required": false
  }
]} />

#### Response

Returns confirmation of failure report.

| Status | Description |
|--------|-------------|
| 200 | Failure reported |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Deployment not found |

#### Example

<ApiExample method="POST" path="/cli/deploy/2/fail/dep_abc123" body={{
  "error": "Build failed: TypeScript compilation error"
}} />

---

## Hostname

### Get Hostname

Get the vanity hostname for a project.

<ApiEndpoint method="GET" path="/cli/project/{id}/hostname" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Response

Returns the vanity hostname and URL.

| Status | Description |
|--------|-------------|
| 200 | Hostname returned |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "hostname",
    "type": "string",
    "description": "Vanity hostname or null"
  },
  {
    "name": "url",
    "type": "string",
    "description": "Full URL or null"
  }
]} />

#### Example

<ApiExample method="GET" path="/cli/project/proj_abc123/hostname" />

---

### Set Hostname

Set a vanity hostname for a project.

<ApiEndpoint method="PUT" path="/cli/project/{id}/hostname" />

#### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Project ID"
  }
]} />

#### Request Body

Hostname configuration payload.

<ResponseFields fields={[
  {
    "name": "hostname",
    "type": "string",
    "description": "Desired vanity hostname",
    "required": true
  }
]} />

#### Response

Returns the configured hostname and URL.

| Status | Description |
|--------|-------------|
| 200 | Hostname set |
| 401 | Unauthorized — invalid or missing API key |
| 404 | Project not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "hostname",
    "type": "string",
    "description": "Configured hostname"
  },
  {
    "name": "url",
    "type": "string",
    "description": "Full URL"
  }
]} />

#### Example

<ApiExample method="PUT" path="/cli/project/proj_abc123/hostname" body={{
  "hostname": "my-app"
}} />

---

## Security

### Check for Malware

Scan deployment dependencies for known malware.

<ApiEndpoint method="POST" path="/security/{deploymentId}/malware-check" />

#### Parameters

<ParamTable params={[
  {
    "name": "deploymentId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Deployment ID (URL-encoded)"
  }
]} />

#### Request Body

Malware scan payload.

<ResponseFields fields={[
  {
    "name": "ecosystem",
    "type": "string",
    "description": "\\`npm\\`",
    "required": true
  },
  {
    "name": "packages",
    "type": "array",
    "description": "Array of { name, version }",
    "required": true
  }
]} />

#### Response

Returns scan results. If action is 'block', the deployment should be blocked.

| Status | Description |
|--------|-------------|
| 200 | Scan completed |
| 401 | Unauthorized — invalid or missing API key |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "action",
    "type": "string",
    "description": "\\`allow\\` or \\`block\\`"
  },
  {
    "name": "summary",
    "type": "object",
    "description": "{ scanned, flagged }"
  },
  {
    "name": "findings",
    "type": "array",
    "description": "Array of { name, version, reason }"
  }
]} />

#### Example

<ApiExample method="POST" path="/security/dep_abc123/malware-check" body={{
  "ecosystem": "npm",
  "packages": [
    {
      "name": "lodash",
      "version": "4.17.21"
    }
  ]
}} />

---
