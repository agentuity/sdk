---
title: Vector Search API
description: Semantic search with automatic embedding generation
---

{/* This file is auto-generated from Zod schemas. Do not edit manually. Run scripts/generate-api-reference.ts to regenerate. */}


<RegionPicker />

## Authentication

All requests require a Bearer token. Pass your SDK key in the `Authorization` header.

| Header | Value |
|--------|-------|
| `Authorization` | `Bearer YOUR_SDK_KEY` |

You can find your SDK key in the [Agentuity Console](https://app.agentuity.com) under your project settings.

---

## Upsert Vectors

Create or update vectors in a namespace. Provide either `document` (auto-embeds) or pre-computed `embeddings` per item.

<ApiEndpoint method="PUT" path="/vector/{namespace}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace name"
  }
]} />

### Request Body

JSON array of vector documents to upsert.

<ResponseFields fields={[
  {
    "name": "key",
    "type": "string",
    "description": "the key of the vector object which can be used as a reference. the value of this key is opaque to the vector storage.",
    "required": true
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "the metadata to upsert",
    "required": false
  },
  {
    "name": "ttl",
    "type": "number | null",
    "description": "Time-to-live in seconds for the vector. Controls when the vector expires and is automatically deleted.",
    "required": false
  }
]} />

### Response

JSON response with inserted/updated vector IDs.

| Status | Description |
|--------|-------------|
| 200 | Vectors upserted successfully |
| 401 | Unauthorized — invalid or missing API key |
| 402 | Payment required — upgrade to a paid plan |

### Response Fields

<ResponseFields fields={[
  {
    "name": "success",
    "type": "boolean",
    "description": "Whether the request succeeded",
    "required": true
  },
  {
    "name": "data",
    "type": "object[]",
    "description": "Array of upserted vector results",
    "required": true
  },
  {
    "name": "data[].id",
    "type": "string",
    "description": "Stored vector ID",
    "required": true
  }
]} />

### Notes

TTL behavior:
- **Omitted**: Vector expires after 30 days (default)
- **`null` or `0`**: Vector never expires
- **60–7,776,000**: Expires after specified seconds (values outside range are clamped)

### Example

<ApiExample method="PUT" path="/vector/products" body={[
  {
    "key": "chair-001",
    "document": "Comfortable office chair with lumbar support",
    "metadata": {
      "category": "furniture",
      "price": 299
    }
  }
]} />

---

## Get Vector

Fetch a vector by namespace and key.

<ApiEndpoint method="GET" path="/vector/{namespace}/{key}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace name"
  },
  {
    "name": "key",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The vector key"
  }
]} />

### Response

JSON response with vector data, metadata, similarity, and expiration fields.

| Status | Description |
|--------|-------------|
| 200 | Vector found and returned |
| 401 | Unauthorized |
| 404 | Vector not found |

### Response Fields

<ResponseFields fields={[
  {
    "name": "success",
    "type": "boolean",
    "description": "Whether the request succeeded",
    "required": true
  },
  {
    "name": "data",
    "type": "object",
    "description": "The vector data object",
    "required": true
  },
  {
    "name": "data.id",
    "type": "string",
    "description": "Internal vector ID",
    "required": true
  },
  {
    "name": "data.key",
    "type": "string",
    "description": "Vector key",
    "required": true
  },
  {
    "name": "data.document",
    "type": "string",
    "description": "Original source document text",
    "required": false
  },
  {
    "name": "data.embeddings",
    "type": "number[]",
    "description": "Stored embeddings array",
    "required": false
  },
  {
    "name": "data.metadata",
    "type": "object",
    "description": "Stored metadata",
    "required": false
  },
  {
    "name": "data.similarity",
    "type": "number",
    "description": "Similarity score when relevant",
    "required": true
  },
  {
    "name": "data.expiresAt",
    "type": "string | null",
    "description": "ISO 8601 expiration timestamp",
    "required": false
  }
]} />

### Example

<ApiExample method="GET" path="/vector/products/chair-001" />

---

## Search Vectors

Run semantic similarity search in a namespace.

<ApiEndpoint method="POST" path="/vector/search/{namespace}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace to search"
  }
]} />

### Request Body

Search criteria and filters.

<ResponseFields fields={[
  {
    "name": "query",
    "type": "string",
    "description": "The text query to search for in the vector storage. This will be converted to embeddings and used to find semantically similar documents.",
    "required": true
  },
  {
    "name": "limit",
    "type": "number",
    "description": "Maximum number of search results to return. If not specified, the server default will be used.",
    "required": false
  },
  {
    "name": "similarity",
    "type": "number",
    "description": "Minimum similarity threshold for results. Only vectors with similarity scores greater than or equal to this value will be returned.",
    "required": false
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "Metadata filters to apply to the search. Only vectors whose metadata matches all specified key-value pairs will be included in results.",
    "required": false
  }
]} />

### Response

JSON response containing matching vectors and similarity scores.

| Status | Description |
|--------|-------------|
| 200 | Search results returned |
| 401 | Unauthorized |
| 404 | Namespace not found (returns empty array) |

### Response Fields

<ResponseFields fields={[
  {
    "name": "success",
    "type": "boolean",
    "description": "Whether the request succeeded",
    "required": true
  },
  {
    "name": "data",
    "type": "object[]",
    "description": "Array of matching vector results",
    "required": true
  },
  {
    "name": "data[].id",
    "type": "string",
    "description": "Vector ID",
    "required": true
  },
  {
    "name": "data[].key",
    "type": "string",
    "description": "Vector key",
    "required": true
  },
  {
    "name": "data[].metadata",
    "type": "object",
    "description": "Vector metadata",
    "required": false
  },
  {
    "name": "data[].similarity",
    "type": "number",
    "description": "Similarity score (0–1)",
    "required": true
  },
  {
    "name": "data[].expiresAt",
    "type": "string | null",
    "description": "ISO 8601 expiration timestamp",
    "required": false
  }
]} />

### Example

<ApiExample method="POST" path="/vector/search/products" body={{
  "query": "comfortable seating for office",
  "limit": 5,
  "similarity": 0.7
}} />

---

## Delete Vector

Delete a single vector by key.

<ApiEndpoint method="DELETE" path="/vector/{namespace}/{key}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace name"
  },
  {
    "name": "key",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The vector key to delete"
  }
]} />

### Response

JSON response with deleted row count.

| Status | Description |
|--------|-------------|
| 200 | Delete operation completed |
| 401 | Unauthorized |
| 404 | Vector not found (returns 0) |

### Response Fields

<ResponseFields fields={[
  {
    "name": "success",
    "type": "boolean",
    "description": "Whether the request succeeded",
    "required": true
  },
  {
    "name": "data",
    "type": "number",
    "description": "Number of deleted vectors",
    "required": true
  }
]} />

### Example

<ApiExample method="DELETE" path="/vector/products/chair-001" />

---

## Delete Multiple Vectors

Delete multiple vectors by key. If no body is provided, deletes the entire namespace.

<ApiEndpoint method="DELETE" path="/vector/{namespace}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace name"
  }
]} />

### Request Body

Optional JSON body specifying keys to delete.

<ResponseFields fields={[
  {
    "name": "keys",
    "type": "string[]",
    "description": "Vector keys to delete"
  }
]} />

### Response

JSON response with number of deleted vectors.

| Status | Description |
|--------|-------------|
| 200 | Delete operation completed |
| 401 | Unauthorized |

### Response Fields

<ResponseFields fields={[
  {
    "name": "success",
    "type": "boolean",
    "description": "Whether the request succeeded",
    "required": true
  },
  {
    "name": "data",
    "type": "number",
    "description": "Number of deleted vectors",
    "required": true
  }
]} />

### Example

<ApiExample method="DELETE" path="/vector/products" body={{
  "keys": [
    "chair-001",
    "desk-001"
  ]
}} />

---

## Get Namespace Stats

Get aggregate stats for a namespace.

<ApiEndpoint method="GET" path="/vector/stats/{namespace}" />

### Parameters

<ParamTable params={[
  {
    "name": "namespace",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "The namespace name"
  }
]} />

### Response

JSON object with namespace size and usage statistics.

| Status | Description |
|--------|-------------|
| 200 | Stats returned |
| 401 | Unauthorized |
| 404 | Namespace not found (returns zeros) |

### Response Fields

<ResponseFields fields={[
  {
    "name": "sum",
    "type": "number",
    "description": "Total size in bytes",
    "required": true
  },
  {
    "name": "count",
    "type": "number",
    "description": "Number of vectors in the namespace",
    "required": true
  },
  {
    "name": "createdAt",
    "type": "number",
    "description": "Unix timestamp (milliseconds) when the namespace was created",
    "required": false
  },
  {
    "name": "lastUsed",
    "type": "number",
    "description": "Unix timestamp (milliseconds) when the namespace was last used",
    "required": false
  },
  {
    "name": "sampledResults",
    "type": "object",
    "description": "A sample of vectors in the namespace (up to 20)",
    "required": false
  }
]} />

### Example

<ApiExample method="GET" path="/vector/stats/products" />

---

## List All Namespace Stats

List namespace stats with pagination, filtering, and sorting.

<ApiEndpoint method="GET" path="/vector/stats" />

### Parameters

<ParamTable params={[
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum namespaces to return (default: 100, max: 1000)"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Offset for pagination"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort by `name`, `size`, `records`, `created`, or `lastUsed`"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort direction: `asc` or `desc`"
  },
  {
    "name": "name",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter namespaces by name"
  }
]} />

### Response

Paginated namespace stats response.

| Status | Description |
|--------|-------------|
| 200 | Stats returned |
| 401 | Unauthorized |

### Response Fields

<ResponseFields fields={[
  {
    "name": "namespaces",
    "type": "object",
    "description": "Map of namespace names to their statistics",
    "required": true
  },
  {
    "name": "total",
    "type": "number",
    "description": "Total number of namespaces across all pages",
    "required": true
  },
  {
    "name": "limit",
    "type": "number",
    "description": "Number of namespaces requested per page",
    "required": true
  },
  {
    "name": "offset",
    "type": "number",
    "description": "Number of namespaces skipped",
    "required": true
  },
  {
    "name": "hasMore",
    "type": "boolean",
    "description": "Whether there are more namespaces available",
    "required": true
  }
]} />

### Example

<ApiExample method="GET" path="/vector/stats?limit=10&sort=size&direction=desc" />

---

## List Namespaces

List namespace names (up to 1000).

<ApiEndpoint method="GET" path="/vector/namespaces" />



### Response

JSON array of namespace names.

| Status | Description |
|--------|-------------|
| 200 | Namespace list returned |
| 401 | Unauthorized |

### Example

<ApiExample method="GET" path="/vector/namespaces" />

---
