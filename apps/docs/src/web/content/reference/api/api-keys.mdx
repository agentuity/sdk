---
title: API Keys API
description: Create and manage API keys for authentication
---

Create and manage API keys for authentication.

<RegionPicker />

## Authentication

All requests require a Bearer token. Pass your SDK key in the `Authorization` header.

| Header | Value |
|--------|-------|
| `Authorization` | `Bearer YOUR_SDK_KEY` |

You can find your SDK key in the [Agentuity Console](https://app.agentuity.com) under your project settings.

---

## Create API Key

Create a new API key. The key value is only returned at creation time.

<ApiEndpoint method="POST" path="/cli/apikey" />



### Request Body

API key creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Name of the API key",
    "required": true
  },
  {
    "name": "expiresAt",
    "type": "string",
    "description": "Expiration timestamp for the API key",
    "required": true
  },
  {
    "name": "projectId",
    "type": "string | null",
    "description": "Optional project scope for the key",
    "required": false
  },
  {
    "name": "orgId",
    "type": "string | null",
    "description": "Optional organization scope for the key",
    "required": false
  }
]} />

### Response

Returns the new key ID and value. The value is only returned at creation time.

| Status | Description |
|--------|-------------|
| 201 | API key created |
| 401 | Unauthorized — invalid or missing Bearer token |

### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "the API key id",
    "required": true
  },
  {
    "name": "value",
    "type": "string",
    "description": "the API key value",
    "required": true
  }
]} />

### Example

<ApiExample method="POST" path="/cli/apikey" body={{
  "name": "My API Key",
  "expiresAt": "2026-12-31T23:59:59Z"
}} />

---

## List API Keys

List API keys with optional filtering by organization or project.

<ApiEndpoint method="GET" path="/cli/apikey" />

### Parameters

<ParamTable params={[
  {
    "name": "orgId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by organization ID"
  },
  {
    "name": "projectId",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by project ID"
  }
]} />

### Response

Array of API key objects.

| Status | Description |
|--------|-------------|
| 200 | API keys returned |
| 401 | Unauthorized — invalid or missing Bearer token |

### Response Fields

<ResponseFields fields={[
  {
    "name": "id",
    "type": "string",
    "description": "the API key id",
    "required": true
  },
  {
    "name": "name",
    "type": "string",
    "description": "the API key name",
    "required": true
  },
  {
    "name": "orgId",
    "type": "string",
    "description": "the organization id",
    "required": true
  },
  {
    "name": "type",
    "type": "string",
    "description": "the API key type",
    "required": true
  },
  {
    "name": "expiresAt",
    "type": "string | null",
    "description": "the expiration date",
    "required": true
  },
  {
    "name": "lastUsedAt",
    "type": "string | null",
    "description": "the last used date",
    "required": false
  },
  {
    "name": "createdAt",
    "type": "string",
    "description": "the creation date",
    "required": true
  },
  {
    "name": "project",
    "type": "object | null",
    "description": "the associated project",
    "required": false
  }
]} />

### Example

<ApiExample method="GET" path="/cli/apikey" />

---

## Get API Key

Get a specific API key by ID.

<ApiEndpoint method="GET" path="/cli/apikey/{id}" />

### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "API key ID"
  }
]} />

### Response

API key object.

| Status | Description |
|--------|-------------|
| 200 | API key returned |
| 401 | Unauthorized — invalid or missing Bearer token |
| 404 | API key not found |

### Example

<ApiExample method="GET" path="/cli/apikey/ak_abc123" />

---

## Delete API Key

Delete an API key by ID.

<ApiEndpoint method="DELETE" path="/cli/apikey/{id}" />

### Parameters

<ParamTable params={[
  {
    "name": "id",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "API key ID"
  }
]} />

### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 204 | API key deleted |
| 401 | Unauthorized — invalid or missing Bearer token |
| 404 | API key not found |

### Example

<ApiExample method="DELETE" path="/cli/apikey/ak_abc123" />

---
