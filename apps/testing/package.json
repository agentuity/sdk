{
	"name": "testing",
	"private": true,
	"scripts": {
		"typecheck": "echo 'No testing apps (moved to legacy)'",
		"build": "echo 'No testing apps (moved to legacy)'",
		"test": "echo 'No testing apps (moved to legacy)'"
	},
	"version": "0.0.75"
}
