---
title: Choosing Authentication
short_title: Authentication
description: Choose between Sign in with Agentuity and app-owned authentication for routes and apps
---

import { KeyRound, Monitor, ShieldCheck, Terminal } from 'lucide-react';

Choose the auth path based on who owns identity. For new apps that should let users sign in with their Agentuity account, start with [Sign in with Agentuity](/services/oidc-provider). Use `@agentuity/auth` when you need app-owned sessions, API keys, or bearer tokens.

## Which Auth Path Should I Use?

| Need | Use | Start Here |
|------|-----|------------|
| Users sign in with their Agentuity account | Agentuity OIDC provider | [Sign in with Agentuity](/services/oidc-provider) |
| Users grant scoped access to Agentuity resources | Agentuity OIDC provider and OAuth scopes | [Sign in with Agentuity](/services/oidc-provider#scopes-and-consent) |
| Your app owns users, sessions, API keys, or bearer tokens | `@agentuity/auth` | [Quick Start](#quick-start-with-agentuityauth) |
| Manage OAuth app credentials and scopes | CLI or REST API | [CLI OAuth commands](/reference/cli/oauth) |

## Quick Start with `@agentuity/auth`

Install the auth and drizzle packages:

```bash
bun add @agentuity/auth @agentuity/drizzle
```

<Tabs items={["Server Setup", "Agent Handler"]}>
<Tab value="Server Setup">
Create an auth instance, mount the auth routes, and protect your API with session middleware:

```typescript title="src/api/index.ts"
import { Hono } from 'hono';
import type { Env } from '@agentuity/runtime';
import { createAuth, mountAuthRoutes, createSessionMiddleware } from '@agentuity/auth';

const auth = createAuth({
  connectionString: process.env.DATABASE_URL,
});

const router = new Hono<Env>();

// Expose sign-in, sign-up, session, and token endpoints
router.on(['GET', 'POST'], '/api/auth/*', mountAuthRoutes(auth));

// Require a valid session for all other API routes
router.use('/api/*', createSessionMiddleware(auth));

export default router;
```
</Tab>
<Tab value="Agent Handler">
When middleware is configured, `ctx.auth` is available in every agent handler:

```typescript title="src/agent/protected/agent.ts"
import { createAgent } from '@agentuity/runtime';

export default createAgent('protected', {
  handler: async (ctx, input) => {
    if (!ctx.auth) {
      ctx.logger.warn('unauthenticated request');
      return { error: 'Please sign in' };
    }

    const user = await ctx.auth.getUser();
    ctx.logger.info('authenticated request', { userId: user.id });

    return { greeting: `Hello, ${user.name}` };
  },
});
```
</Tab>
</Tabs>

## Auth Methods

| Method | When to Use | How It Works |
|--------|------------|--------------|
| Session | Browser users with a login flow | Cookie-based, managed by BetterAuth |
| API Key | Server-to-server or CLI tools | Header: `x-agentuity-auth-api-key` or `Authorization: ApiKey <key>` |
| Bearer Token | JWT-based service auth | Header: `Authorization: Bearer <token>` |

You can check which method authenticated a request with `ctx.auth.authMethod`, which returns `'session'`, `'api-key'`, or `'bearer'`.

## Auth in Different Contexts

<Cards>
  <CardLink
    href="/services/oidc-provider"
    title="Sign in with Agentuity"
    description="Let users authenticate with Agentuity and grant scoped access"
    icon={<KeyRound />}
  />
  <CardLink
    href="/frontend/authentication"
    title="React Frontend"
    description="AuthProvider, useAuth hooks, and login forms for browser apps"
    icon={<Monitor />}
  />
  <CardLink
    href="/reference/sdk-reference/context-api#authentication"
    title="Agent Handlers"
    description="Full ctx.auth reference with method signatures and return types"
    icon={<ShieldCheck />}
  />
  <CardLink
    href="/reference/cli/oauth"
    title="CLI OAuth"
    description="Create and manage OAuth/OIDC applications from the command line"
    icon={<Terminal />}
  />
</Cards>

## Default Plugins

Agentuity Auth includes four plugins automatically:

| Plugin | Purpose |
|--------|---------|
| `organization` | Multi-tenancy with teams, roles, and invitations |
| `jwt` | JWT token generation with a JWKS endpoint |
| `bearer` | Bearer token auth via the `Authorization` header |
| `apiKey` | API key authentication for programmatic access |

<Callout type="tip" title="Full Control Over Plugins">
Pass `skipDefaultPlugins: true` to `createAuth()` and add only the plugins you need. This is useful when you want a minimal setup or need to customize plugin options.
</Callout>

## Database Setup

Agentuity Auth needs a PostgreSQL database to store users, sessions, and keys.

**Connection string (simplest).** Provide a URL and Agentuity creates the Bun SQL connection and Drizzle adapter internally:

```typescript
import { createAuth } from '@agentuity/auth';

const auth = createAuth({
  connectionString: process.env.DATABASE_URL,
});
```

**Bring your own adapter.** If you already have a Drizzle setup, pass it directly:

```typescript
import { createAuth } from '@agentuity/auth';
import { drizzleAdapter } from '@agentuity/drizzle';
import { drizzle } from 'drizzle-orm/bun-sql';
import * as authSchema from '@agentuity/auth/schema';
import * as appSchema from './schema';

function requireEnv(name: string): string {
  const value = process.env[name];
  if (!value) {
    throw new Error(`${name} is required`);
  }
  return value;
}

const schema = { ...authSchema, ...appSchema };
const db = drizzle(requireEnv('DATABASE_URL'), { schema });

const auth = createAuth({
  database: drizzleAdapter(db, { provider: 'pg', schema: authSchema }),
});
```

<Callout type="info" title="Schema Exports">
The `@agentuity/auth/schema` export provides all auth-related Drizzle tables: `user`, `session`, `account`, `verification`, `organization`, `member`, `invitation`, and `apiKey`.
</Callout>

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `DATABASE_URL` | PostgreSQL connection string | **Required** |
| `AGENTUITY_AUTH_SECRET` | Secret for signing tokens | Falls back to `BETTER_AUTH_SECRET` |
| `AGENTUITY_CLOUD_BASE_URL` | Platform-provided base URL for auth callbacks | Checked before `AGENTUITY_BASE_URL` |
| `AGENTUITY_BASE_URL` | Base URL for auth callbacks | Checked before `BETTER_AUTH_URL` |

By default, trusted origins include the resolved base URL, `AGENTUITY_BASE_URL`, the incoming request origin, `AGENTUITY_CLOUD_DOMAINS`, and `AUTH_TRUSTED_DOMAINS`. Passing `trustedOrigins` to `createAuth()` replaces that default list.

<Callout type="warning" title="Replace Dev Secrets Before Deploying">
Generate a secure secret with `openssl rand -hex 32` and store it in your environment variables before deploying.
</Callout>

## Sign in with Agentuity

Use the Agentuity OIDC provider when your app should redirect users to Agentuity, receive an authorization code, and create its own local app session from the returned profile. The provider handles identity, consent, OAuth apps, scopes, and tokens.

See [Sign in with Agentuity](/services/oidc-provider) for the route example, token storage pattern, and OAuth app setup.

## Next Steps

- [Sign in with Agentuity](/services/oidc-provider): Add Agentuity account sign-in and scoped access
- [React Frontend Auth](/frontend/authentication): AuthProvider setup, useAuth hooks, and sign-in/sign-up forms
- [ctx.auth Reference](/reference/sdk-reference/context-api#authentication): Complete method signatures and return types
- [CLI OAuth Commands](/reference/cli/oauth): Create and manage OAuth/OIDC applications
