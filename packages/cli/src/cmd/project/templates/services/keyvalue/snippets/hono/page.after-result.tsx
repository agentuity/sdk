const prefsNote = document.createElement('p');
prefsNote.className = 'text-[11px] text-gray-600';
prefsNote.innerHTML = 'Preferences powered by <code class="text-gray-500">@agentuity/keyvalue</code>';
resultDiv.appendChild(prefsNote);
