---
title: Message Queues API
description: Publish, consume, and manage messages with worker and pub/sub queues
---

Publish, consume, and manage messages with worker and pub/sub queues.

<RegionPicker />

## Authentication

All requests require a Bearer token. Pass your SDK key in the `Authorization` header.

| Header | Value |
|--------|-------|
| `Authorization` | `Bearer YOUR_SDK_KEY` |

You can find your SDK key in the [Agentuity Console](https://app.agentuity.com) under your project settings.

---

## Queue Management

### Create Queue

Create a queue. If `name` is omitted, a name is auto-generated.

<ApiEndpoint method="POST" path="/queue/create" />



#### Request Body

Queue creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Optional queue name (auto-generated if not provided).",
    "required": false
  },
  {
    "name": "description",
    "type": "string",
    "description": "Optional description of the queue's purpose.",
    "required": false
  },
  {
    "name": "queue_type",
    "type": "string",
    "description": "Type of queue to create.",
    "required": true
  },
  {
    "name": "settings",
    "type": "object",
    "description": "Optional settings to customize queue behavior (server applies defaults for missing fields).",
    "required": false
  },
  {
    "name": "settings.default_ttl_seconds",
    "type": "number | null",
    "description": "Default time-to-live for messages in seconds. Null means no expiration.",
    "required": false
  },
  {
    "name": "settings.default_visibility_timeout_seconds",
    "type": "number",
    "description": "Time in seconds a message is invisible after being received.",
    "required": false
  },
  {
    "name": "settings.default_max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts before moving to DLQ.",
    "required": false
  },
  {
    "name": "settings.default_retry_backoff_ms",
    "type": "number",
    "description": "Initial backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "settings.default_retry_max_backoff_ms",
    "type": "number",
    "description": "Maximum backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "settings.default_retry_multiplier",
    "type": "number",
    "description": "Multiplier for exponential backoff.",
    "required": false
  },
  {
    "name": "settings.max_in_flight_per_client",
    "type": "number",
    "description": "Maximum number of messages a single client can process concurrently.",
    "required": false
  },
  {
    "name": "settings.retention_seconds",
    "type": "number",
    "description": "Retention period for acknowledged messages in seconds.",
    "required": false
  }
]} />

#### Response

Queue object including settings, stats, and timestamps.

| Status | Description |
|--------|-------------|
| 200 | Queue created |
| 401 | Unauthorized |
| 402 | Payment required |

#### Example

<ApiExample method="POST" path="/queue/create" body={{
  "name": "order-processing",
  "queue_type": "worker",
  "description": "Processes customer orders",
  "settings": {
    "default_max_retries": 3,
    "default_visibility_timeout_seconds": 60
  }
}} />

---

### Get Queue

Get a queue by name.

<ApiEndpoint method="GET" path="/queue/get/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

Queue object.

| Status | Description |
|--------|-------------|
| 200 | Queue returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="GET" path="/queue/get/order-processing" />

---

### List Queues

List queues with filtering and pagination.

<ApiEndpoint method="GET" path="/queue/list" />

#### Parameters

<ParamTable params={[
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum queues to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Offset for pagination"
  },
  {
    "name": "name",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Filter by queue name"
  },
  {
    "name": "queue_type",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "`worker` or `pubsub`"
  },
  {
    "name": "status",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "`active` or `paused`"
  },
  {
    "name": "sort",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Sort field"
  },
  {
    "name": "direction",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "`asc` or `desc`"
  }
]} />

#### Response

Response with queue array and optional total count.

| Status | Description |
|--------|-------------|
| 200 | Queues returned |
| 401 | Unauthorized |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "queues",
    "type": "object[]",
    "description": "Queues in the current page",
    "required": true
  },
  {
    "name": "queues[].id",
    "type": "string",
    "description": "Unique identifier for the queue.",
    "required": true
  },
  {
    "name": "queues[].name",
    "type": "string",
    "description": "Human-readable queue name (used for API operations).",
    "required": true
  },
  {
    "name": "queues[].description",
    "type": "string | null",
    "description": "Optional description of the queue's purpose.",
    "required": false
  },
  {
    "name": "queues[].internal",
    "type": "boolean",
    "description": "Whether the queue is system-managed.",
    "required": false
  },
  {
    "name": "queues[].queue_type",
    "type": "string",
    "description": "The type of queue (worker or pubsub).",
    "required": true
  },
  {
    "name": "queues[].default_ttl_seconds",
    "type": "number | null",
    "description": "Default time-to-live for messages in seconds. Null means no expiration.",
    "required": false
  },
  {
    "name": "queues[].default_visibility_timeout_seconds",
    "type": "number",
    "description": "Time in seconds a message is invisible after being received.",
    "required": false
  },
  {
    "name": "queues[].default_max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts before moving to DLQ.",
    "required": false
  },
  {
    "name": "queues[].default_retry_backoff_ms",
    "type": "number",
    "description": "Initial backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "queues[].default_retry_max_backoff_ms",
    "type": "number",
    "description": "Maximum backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "queues[].default_retry_multiplier",
    "type": "number",
    "description": "Multiplier for exponential backoff.",
    "required": false
  },
  {
    "name": "queues[].max_in_flight_per_client",
    "type": "number",
    "description": "Maximum number of messages a single client can process concurrently.",
    "required": false
  },
  {
    "name": "queues[].next_offset",
    "type": "number",
    "description": "The next offset that will be assigned to a new message.",
    "required": false
  },
  {
    "name": "queues[].message_count",
    "type": "number",
    "description": "Total number of messages currently in the queue.",
    "required": false
  },
  {
    "name": "queues[].dlq_count",
    "type": "number",
    "description": "Number of messages in the dead letter queue.",
    "required": false
  },
  {
    "name": "queues[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the queue was created.",
    "required": true
  },
  {
    "name": "queues[].updated_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the queue was last updated.",
    "required": true
  },
  {
    "name": "queues[].paused_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the queue was paused (null if not paused).",
    "required": false
  },
  {
    "name": "queues[].retention_seconds",
    "type": "number",
    "description": "Retention period for acknowledged messages in seconds.",
    "required": false
  },
  {
    "name": "total",
    "type": "number",
    "description": "Optional total queue count",
    "required": false
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/list?limit=10&queue_type=worker" />

---

### Update Queue

Partially update queue description/settings.

<ApiEndpoint method="PATCH" path="/queue/update/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Request Body

Partial queue update payload.

<ResponseFields fields={[
  {
    "name": "description",
    "type": "string",
    "description": "New description for the queue.",
    "required": false
  },
  {
    "name": "settings",
    "type": "object",
    "description": "Settings to update (partial update supported, only provided fields are updated).",
    "required": false
  },
  {
    "name": "settings.default_ttl_seconds",
    "type": "number | null",
    "description": "Default time-to-live for messages in seconds. Null means no expiration.",
    "required": false
  },
  {
    "name": "settings.default_visibility_timeout_seconds",
    "type": "number",
    "description": "Time in seconds a message is invisible after being received.",
    "required": false
  },
  {
    "name": "settings.default_max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts before moving to DLQ.",
    "required": false
  },
  {
    "name": "settings.default_retry_backoff_ms",
    "type": "number",
    "description": "Initial backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "settings.default_retry_max_backoff_ms",
    "type": "number",
    "description": "Maximum backoff delay in milliseconds for retries.",
    "required": false
  },
  {
    "name": "settings.default_retry_multiplier",
    "type": "number",
    "description": "Multiplier for exponential backoff.",
    "required": false
  },
  {
    "name": "settings.max_in_flight_per_client",
    "type": "number",
    "description": "Maximum number of messages a single client can process concurrently.",
    "required": false
  },
  {
    "name": "settings.retention_seconds",
    "type": "number",
    "description": "Retention period for acknowledged messages in seconds.",
    "required": false
  }
]} />

#### Response

Updated queue object.

| Status | Description |
|--------|-------------|
| 200 | Queue updated |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="PATCH" path="/queue/update/order-processing" body={{
  "description": "Updated",
  "settings": {
    "default_max_retries": 5
  }
}} />

---

### Delete Queue

Delete a queue and associated resources.

<ApiEndpoint method="DELETE" path="/queue/delete/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | Queue deleted |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="DELETE" path="/queue/delete/order-processing" />

---

### Pause Queue

Pause queue processing.

<ApiEndpoint method="POST" path="/queue/pause/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Request Body

Empty JSON object is required.

#### Response

Queue object with `paused_at` set.

| Status | Description |
|--------|-------------|
| 200 | Queue paused |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="POST" path="/queue/pause/order-processing" body={{}} />

---

### Resume Queue

Resume queue processing.

<ApiEndpoint method="POST" path="/queue/resume/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Request Body

Empty JSON object is required.

#### Response

Queue object with `paused_at` cleared.

| Status | Description |
|--------|-------------|
| 200 | Queue resumed |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="POST" path="/queue/resume/order-processing" body={{}} />

---

## Message Operations

### Publish Message

Publish a single message to a queue.

<ApiEndpoint method="POST" path="/queue/messages/publish/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "sync",
    "type": "boolean",
    "in": "query",
    "required": false,
    "description": "Set `true` for synchronous publish"
  }
]} />

#### Request Body

Message payload and optional delivery controls.

<ResponseFields fields={[
  {
    "name": "payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "metadata",
    "type": "object",
    "description": "Optional metadata to attach to the message.",
    "required": false
  },
  {
    "name": "idempotency_key",
    "type": "string",
    "description": "Optional key for deduplication (prevents duplicate messages).",
    "required": false
  },
  {
    "name": "partition_key",
    "type": "string",
    "description": "Optional key for message ordering within a partition.",
    "required": false
  },
  {
    "name": "ttl_seconds",
    "type": "number",
    "description": "Optional time-to-live in seconds.",
    "required": false
  }
]} />

#### Response

Published message object.

| Status | Description |
|--------|-------------|
| 200 | Message published |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="POST" path="/queue/messages/publish/order-processing" body={{
  "payload": {
    "orderId": 123,
    "action": "process"
  },
  "metadata": {
    "priority": "high"
  }
}} />

---

### Batch Publish

Publish up to 1000 messages in one request.

<ApiEndpoint method="POST" path="/queue/messages/batch/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Request Body

Batch publish payload.

<ResponseFields fields={[
  {
    "name": "messages",
    "type": "object[]",
    "description": "Array of messages to publish (max 1000 per batch).",
    "required": true
  },
  {
    "name": "messages[].payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "messages[].metadata",
    "type": "object",
    "description": "Optional metadata to attach to the message.",
    "required": false
  },
  {
    "name": "messages[].idempotency_key",
    "type": "string",
    "description": "Optional key for deduplication (prevents duplicate messages).",
    "required": false
  },
  {
    "name": "messages[].partition_key",
    "type": "string",
    "description": "Optional key for message ordering within a partition.",
    "required": false
  },
  {
    "name": "messages[].ttl_seconds",
    "type": "number",
    "description": "Optional time-to-live in seconds.",
    "required": false
  }
]} />

#### Response

Batch publish response with created messages and optional failed indexes.

| Status | Description |
|--------|-------------|
| 200 | Batch publish completed |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "messages",
    "type": "object[]",
    "description": "Published messages",
    "required": true
  },
  {
    "name": "messages[].id",
    "type": "string",
    "description": "Unique identifier for the message (prefixed with qmsg_).",
    "required": true
  },
  {
    "name": "messages[].queue_id",
    "type": "string",
    "description": "ID of the queue this message belongs to.",
    "required": true
  },
  {
    "name": "messages[].offset",
    "type": "number",
    "description": "Sequential offset within the queue.",
    "required": true
  },
  {
    "name": "messages[].payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "messages[].size",
    "type": "number",
    "description": "Size of the message payload in bytes.",
    "required": false
  },
  {
    "name": "messages[].metadata",
    "type": "object | null",
    "description": "Optional metadata attached to the message.",
    "required": false
  },
  {
    "name": "messages[].state",
    "type": "string",
    "description": "Current state of the message.",
    "required": false
  },
  {
    "name": "messages[].idempotency_key",
    "type": "string | null",
    "description": "Optional key for message deduplication.",
    "required": false
  },
  {
    "name": "messages[].partition_key",
    "type": "string | null",
    "description": "Optional key for message ordering.",
    "required": false
  },
  {
    "name": "messages[].ttl_seconds",
    "type": "number | null",
    "description": "Time-to-live in seconds (null means no expiration).",
    "required": false
  },
  {
    "name": "messages[].delivery_attempts",
    "type": "number",
    "description": "Number of times delivery has been attempted.",
    "required": false
  },
  {
    "name": "messages[].max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts allowed.",
    "required": false
  },
  {
    "name": "messages[].published_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was published.",
    "required": false
  },
  {
    "name": "messages[].expires_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message will expire (if TTL set).",
    "required": false
  },
  {
    "name": "messages[].delivered_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was delivered.",
    "required": false
  },
  {
    "name": "messages[].acknowledged_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was acknowledged.",
    "required": false
  },
  {
    "name": "messages[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was created.",
    "required": false
  },
  {
    "name": "messages[].updated_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was last updated.",
    "required": false
  },
  {
    "name": "messages[].source_id",
    "type": "string | null",
    "description": "ID of the source that ingested this message (null if published directly).",
    "required": false
  },
  {
    "name": "messages[].source_name",
    "type": "string | null",
    "description": "Name of the source that ingested this message.",
    "required": false
  },
  {
    "name": "failed",
    "type": "number[]",
    "description": "Indexes that failed",
    "required": false
  }
]} />

#### Example

<ApiExample method="POST" path="/queue/messages/batch/order-processing" body={{
  "messages": [
    {
      "payload": {
        "orderId": 1
      }
    },
    {
      "payload": {
        "orderId": 2
      }
    }
  ]
}} />

---

### Get Message

Get a message by message ID (`msg_...`).

<ApiEndpoint method="GET" path="/queue/messages/get/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Message ID (`msg_...`)"
  }
]} />

#### Response

Message object.

| Status | Description |
|--------|-------------|
| 200 | Message returned |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="GET" path="/queue/messages/get/order-processing/msg_abc123" />

---

### Get Message by Offset

Get a message by numeric offset.

<ApiEndpoint method="GET" path="/queue/messages/offset/{name}/{offset}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "path",
    "required": true,
    "description": "Message offset"
  }
]} />

#### Response

Message object.

| Status | Description |
|--------|-------------|
| 200 | Message returned |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="GET" path="/queue/messages/offset/order-processing/42" />

---

### List Messages

List messages with pagination and optional state filter.

<ApiEndpoint method="GET" path="/queue/messages/list/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum messages to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Offset for pagination"
  },
  {
    "name": "state",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "`pending`, `processing`, `completed`, `failed`, or `dead`"
  }
]} />

#### Response

Response with message list and optional total count.

| Status | Description |
|--------|-------------|
| 200 | Messages returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "messages",
    "type": "object[]",
    "description": "Messages in current page",
    "required": true
  },
  {
    "name": "messages[].id",
    "type": "string",
    "description": "Unique identifier for the message (prefixed with qmsg_).",
    "required": true
  },
  {
    "name": "messages[].queue_id",
    "type": "string",
    "description": "ID of the queue this message belongs to.",
    "required": true
  },
  {
    "name": "messages[].offset",
    "type": "number",
    "description": "Sequential offset within the queue.",
    "required": true
  },
  {
    "name": "messages[].payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "messages[].size",
    "type": "number",
    "description": "Size of the message payload in bytes.",
    "required": false
  },
  {
    "name": "messages[].metadata",
    "type": "object | null",
    "description": "Optional metadata attached to the message.",
    "required": false
  },
  {
    "name": "messages[].state",
    "type": "string",
    "description": "Current state of the message.",
    "required": false
  },
  {
    "name": "messages[].idempotency_key",
    "type": "string | null",
    "description": "Optional key for message deduplication.",
    "required": false
  },
  {
    "name": "messages[].partition_key",
    "type": "string | null",
    "description": "Optional key for message ordering.",
    "required": false
  },
  {
    "name": "messages[].ttl_seconds",
    "type": "number | null",
    "description": "Time-to-live in seconds (null means no expiration).",
    "required": false
  },
  {
    "name": "messages[].delivery_attempts",
    "type": "number",
    "description": "Number of times delivery has been attempted.",
    "required": false
  },
  {
    "name": "messages[].max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts allowed.",
    "required": false
  },
  {
    "name": "messages[].published_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was published.",
    "required": false
  },
  {
    "name": "messages[].expires_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message will expire (if TTL set).",
    "required": false
  },
  {
    "name": "messages[].delivered_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was delivered.",
    "required": false
  },
  {
    "name": "messages[].acknowledged_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was acknowledged.",
    "required": false
  },
  {
    "name": "messages[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was created.",
    "required": false
  },
  {
    "name": "messages[].updated_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was last updated.",
    "required": false
  },
  {
    "name": "messages[].source_id",
    "type": "string | null",
    "description": "ID of the source that ingested this message (null if published directly).",
    "required": false
  },
  {
    "name": "messages[].source_name",
    "type": "string | null",
    "description": "Name of the source that ingested this message.",
    "required": false
  },
  {
    "name": "total",
    "type": "number",
    "description": "Optional total message count",
    "required": false
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/messages/list/order-processing?limit=20&state=pending" />

---

### Delete Message

Delete a message by ID.

<ApiEndpoint method="DELETE" path="/queue/messages/delete/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Message ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | Message deleted |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="DELETE" path="/queue/messages/delete/order-processing/msg_abc123" />

---

### Replay Message

Replay a message back into the queue flow.

<ApiEndpoint method="POST" path="/queue/messages/replay/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Message ID"
  }
]} />

#### Response

Replayed message object.

| Status | Description |
|--------|-------------|
| 200 | Message replayed |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="POST" path="/queue/messages/replay/order-processing/msg_abc123" />

---

### Consume Messages

Log-style consumption starting from offset. Does not mark messages as processing.

<ApiEndpoint method="GET" path="/queue/consume/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": true,
    "description": "Starting offset"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum messages to return"
  }
]} />

#### Response

Response containing consumed messages.

| Status | Description |
|--------|-------------|
| 200 | Messages consumed |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "messages",
    "type": "object[]",
    "description": "Consumed messages",
    "required": true
  },
  {
    "name": "messages[].id",
    "type": "string",
    "description": "Unique identifier for the message (prefixed with qmsg_).",
    "required": true
  },
  {
    "name": "messages[].queue_id",
    "type": "string",
    "description": "ID of the queue this message belongs to.",
    "required": true
  },
  {
    "name": "messages[].offset",
    "type": "number",
    "description": "Sequential offset within the queue.",
    "required": true
  },
  {
    "name": "messages[].payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "messages[].size",
    "type": "number",
    "description": "Size of the message payload in bytes.",
    "required": false
  },
  {
    "name": "messages[].metadata",
    "type": "object | null",
    "description": "Optional metadata attached to the message.",
    "required": false
  },
  {
    "name": "messages[].state",
    "type": "string",
    "description": "Current state of the message.",
    "required": false
  },
  {
    "name": "messages[].idempotency_key",
    "type": "string | null",
    "description": "Optional key for message deduplication.",
    "required": false
  },
  {
    "name": "messages[].partition_key",
    "type": "string | null",
    "description": "Optional key for message ordering.",
    "required": false
  },
  {
    "name": "messages[].ttl_seconds",
    "type": "number | null",
    "description": "Time-to-live in seconds (null means no expiration).",
    "required": false
  },
  {
    "name": "messages[].delivery_attempts",
    "type": "number",
    "description": "Number of times delivery has been attempted.",
    "required": false
  },
  {
    "name": "messages[].max_retries",
    "type": "number",
    "description": "Maximum number of delivery attempts allowed.",
    "required": false
  },
  {
    "name": "messages[].published_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was published.",
    "required": false
  },
  {
    "name": "messages[].expires_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message will expire (if TTL set).",
    "required": false
  },
  {
    "name": "messages[].delivered_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was delivered.",
    "required": false
  },
  {
    "name": "messages[].acknowledged_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp when the message was acknowledged.",
    "required": false
  },
  {
    "name": "messages[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was created.",
    "required": false
  },
  {
    "name": "messages[].updated_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was last updated.",
    "required": false
  },
  {
    "name": "messages[].source_id",
    "type": "string | null",
    "description": "ID of the source that ingested this message (null if published directly).",
    "required": false
  },
  {
    "name": "messages[].source_name",
    "type": "string | null",
    "description": "Name of the source that ingested this message.",
    "required": false
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/consume/order-processing?offset=0&limit=10" />

---

### Receive Message

Atomically receive and lock the next pending message. Must ack/nack when done.

<ApiEndpoint method="GET" path="/queue/receive/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "timeout",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Long-poll timeout in seconds (0–30)"
  }
]} />

#### Response

Response containing a message or `null` when none available.

| Status | Description |
|--------|-------------|
| 200 | Receive operation completed |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "message",
    "type": "object | null",
    "description": "Received message if available",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/receive/order-processing?timeout=10" />

---

### Acknowledge Message

Mark message as successfully processed (completed).

<ApiEndpoint method="POST" path="/queue/ack/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Message ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | Message acknowledged |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="POST" path="/queue/ack/order-processing/msg_abc123" />

---

### Negative Acknowledge

Return message for retry. After max retries, moves to DLQ.

<ApiEndpoint method="POST" path="/queue/nack/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Message ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | Message negatively acknowledged |
| 401 | Unauthorized |
| 404 | Message or queue not found |

#### Example

<ApiExample method="POST" path="/queue/nack/order-processing/msg_abc123" />

---

### Get Queue Head

Get offset of the oldest message.

<ApiEndpoint method="GET" path="/queue/head/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

JSON object containing the head offset.

| Status | Description |
|--------|-------------|
| 200 | Queue head returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "offset",
    "type": "number",
    "description": "Oldest message offset",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/head/order-processing" />

---

### Get Queue Tail

Get offset of the newest message.

<ApiEndpoint method="GET" path="/queue/tail/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

JSON object containing the tail offset.

| Status | Description |
|--------|-------------|
| 200 | Queue tail returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "offset",
    "type": "number",
    "description": "Newest message offset",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/tail/order-processing" />

---

## Destinations

### Create Destination

Create an HTTP destination for queue deliveries.

<ApiEndpoint method="POST" path="/queue/destinations/create/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Request Body

Destination creation payload.

<ResponseFields fields={[
  {
    "name": "name",
    "type": "string",
    "description": "Human-readable name for the destination.",
    "required": true
  },
  {
    "name": "description",
    "type": "string",
    "description": "Optional description of the destination.",
    "required": false
  },
  {
    "name": "destination_type",
    "type": "string",
    "description": "Type of destination to create.",
    "required": true
  },
  {
    "name": "config",
    "type": "object",
    "description": "HTTP configuration for the destination.",
    "required": true
  },
  {
    "name": "config.url",
    "type": "string",
    "description": "The URL to send messages to.",
    "required": true
  },
  {
    "name": "config.headers",
    "type": "object",
    "description": "Optional custom headers to include in requests.",
    "required": false
  },
  {
    "name": "config.method",
    "type": "string",
    "description": "HTTP method to use (default: POST).",
    "required": true
  },
  {
    "name": "config.timeout_ms",
    "type": "number",
    "description": "Request timeout in milliseconds (default: 30000).",
    "required": true
  },
  {
    "name": "config.retry_policy",
    "type": "object",
    "description": "Optional retry policy for failed deliveries.",
    "required": false
  },
  {
    "name": "config.retry_policy.max_attempts",
    "type": "number",
    "description": "Maximum number of delivery attempts (default: 5).",
    "required": true
  },
  {
    "name": "config.retry_policy.initial_backoff_ms",
    "type": "number",
    "description": "Initial backoff delay in milliseconds (default: 1000).",
    "required": true
  },
  {
    "name": "config.retry_policy.max_backoff_ms",
    "type": "number",
    "description": "Maximum backoff delay in milliseconds (default: 60000).",
    "required": true
  },
  {
    "name": "config.retry_policy.backoff_multiplier",
    "type": "number",
    "description": "Backoff multiplier for exponential backoff (default: 2.0).",
    "required": true
  },
  {
    "name": "config.signing",
    "type": "object",
    "description": "Optional request signing configuration.",
    "required": false
  },
  {
    "name": "config.signing.enabled",
    "type": "boolean",
    "description": "Whether signing is enabled (default: false).",
    "required": true
  },
  {
    "name": "config.signing.secret_key",
    "type": "string",
    "description": "Secret key for HMAC signing.",
    "required": false
  },
  {
    "name": "enabled",
    "type": "boolean",
    "description": "Whether the destination should be enabled (default: true).",
    "required": true
  }
]} />

#### Response

Created destination object.

| Status | Description |
|--------|-------------|
| 200 | Destination created |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="POST" path="/queue/destinations/create/order-processing" body={{
  "destination_type": "http",
  "config": {
    "url": "https://example.com/webhook"
  },
  "retry_attempts": 5,
  "timeout_seconds": 10
}} />

---

### List Destinations

List queue destinations.

<ApiEndpoint method="GET" path="/queue/destinations/list/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

Response containing destination list.

| Status | Description |
|--------|-------------|
| 200 | Destinations returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "destinations",
    "type": "object[]",
    "description": "Configured destinations",
    "required": true
  },
  {
    "name": "destinations[].id",
    "type": "string",
    "description": "Unique identifier for the destination (prefixed with qdest_).",
    "required": true
  },
  {
    "name": "destinations[].name",
    "type": "string",
    "description": "Human-readable name for the destination.",
    "required": true
  },
  {
    "name": "destinations[].description",
    "type": "string | null",
    "description": "Optional description of the destination.",
    "required": false
  },
  {
    "name": "destinations[].queue_id",
    "type": "string",
    "description": "ID of the queue this destination is attached to.",
    "required": true
  },
  {
    "name": "destinations[].destination_type",
    "type": "string",
    "description": "Type of destination (currently only 'http').",
    "required": true
  },
  {
    "name": "destinations[].config",
    "type": "object",
    "description": "HTTP configuration for the destination.",
    "required": true
  },
  {
    "name": "destinations[].config.url",
    "type": "string",
    "description": "The URL to send messages to.",
    "required": true
  },
  {
    "name": "destinations[].config.headers",
    "type": "object",
    "description": "Optional custom headers to include in requests.",
    "required": false
  },
  {
    "name": "destinations[].config.method",
    "type": "string",
    "description": "HTTP method to use (default: POST).",
    "required": true
  },
  {
    "name": "destinations[].config.timeout_ms",
    "type": "number",
    "description": "Request timeout in milliseconds (default: 30000).",
    "required": true
  },
  {
    "name": "destinations[].config.retry_policy",
    "type": "object",
    "description": "Optional retry policy for failed deliveries.",
    "required": false
  },
  {
    "name": "destinations[].config.retry_policy.max_attempts",
    "type": "number",
    "description": "Maximum number of delivery attempts (default: 5).",
    "required": true
  },
  {
    "name": "destinations[].config.retry_policy.initial_backoff_ms",
    "type": "number",
    "description": "Initial backoff delay in milliseconds (default: 1000).",
    "required": true
  },
  {
    "name": "destinations[].config.retry_policy.max_backoff_ms",
    "type": "number",
    "description": "Maximum backoff delay in milliseconds (default: 60000).",
    "required": true
  },
  {
    "name": "destinations[].config.retry_policy.backoff_multiplier",
    "type": "number",
    "description": "Backoff multiplier for exponential backoff (default: 2.0).",
    "required": true
  },
  {
    "name": "destinations[].config.signing",
    "type": "object",
    "description": "Optional request signing configuration.",
    "required": false
  },
  {
    "name": "destinations[].config.signing.enabled",
    "type": "boolean",
    "description": "Whether signing is enabled (default: false).",
    "required": true
  },
  {
    "name": "destinations[].config.signing.secret_key",
    "type": "string",
    "description": "Secret key for HMAC signing.",
    "required": false
  },
  {
    "name": "destinations[].enabled",
    "type": "boolean",
    "description": "Whether the destination is enabled for delivery.",
    "required": true
  },
  {
    "name": "destinations[].stats",
    "type": "object",
    "description": "Delivery statistics for this destination.",
    "required": false
  },
  {
    "name": "destinations[].stats.total_deliveries",
    "type": "number",
    "description": "Total number of delivery attempts.",
    "required": true
  },
  {
    "name": "destinations[].stats.successful_deliveries",
    "type": "number",
    "description": "Number of successful deliveries.",
    "required": true
  },
  {
    "name": "destinations[].stats.failed_deliveries",
    "type": "number",
    "description": "Number of failed deliveries.",
    "required": true
  },
  {
    "name": "destinations[].stats.last_delivery_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp of the last delivery attempt.",
    "required": false
  },
  {
    "name": "destinations[].stats.last_success_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp of the last successful delivery.",
    "required": false
  },
  {
    "name": "destinations[].stats.last_failure_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp of the last failed delivery.",
    "required": false
  },
  {
    "name": "destinations[].success_count",
    "type": "number",
    "description": "Total successful deliveries.",
    "required": false
  },
  {
    "name": "destinations[].failure_count",
    "type": "number",
    "description": "Total failed deliveries.",
    "required": false
  },
  {
    "name": "destinations[].last_success_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp of last success.",
    "required": false
  },
  {
    "name": "destinations[].last_failure_at",
    "type": "string | null",
    "description": "ISO 8601 timestamp of last failure.",
    "required": false
  },
  {
    "name": "destinations[].last_failure_error",
    "type": "string | null",
    "description": "Error message from last failure.",
    "required": false
  },
  {
    "name": "destinations[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the destination was created.",
    "required": true
  },
  {
    "name": "destinations[].updated_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the destination was last updated.",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/destinations/list/order-processing" />

---

### Update Destination

Partially update destination fields.

<ApiEndpoint method="PATCH" path="/queue/destinations/update/{name}/{destinationId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "destinationId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Destination ID"
  }
]} />

#### Request Body

Partial destination update payload.

#### Response

Updated destination object.

| Status | Description |
|--------|-------------|
| 200 | Destination updated |
| 401 | Unauthorized |
| 404 | Queue or destination not found |

#### Example

<ApiExample method="PATCH" path="/queue/destinations/update/order-processing/dst_abc123" body={{
  "timeout_seconds": 20
}} />

---

### Delete Destination

Delete a queue destination.

<ApiEndpoint method="DELETE" path="/queue/destinations/delete/{name}/{destinationId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "destinationId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Destination ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | Destination deleted |
| 401 | Unauthorized |
| 404 | Queue or destination not found |

#### Example

<ApiExample method="DELETE" path="/queue/destinations/delete/order-processing/dst_abc123" />

---

### List Delivery Logs

List destination delivery logs with optional status filter.

<ApiEndpoint method="GET" path="/queue/destinations/deliveries/{name}/{destinationId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "destinationId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Destination ID"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum logs to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Offset for pagination"
  },
  {
    "name": "status",
    "type": "string",
    "in": "query",
    "required": false,
    "description": "Delivery status filter"
  }
]} />

#### Response

Response containing delivery log entries.

| Status | Description |
|--------|-------------|
| 200 | Delivery logs returned |
| 401 | Unauthorized |
| 404 | Queue or destination not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "deliveries",
    "type": "object[]",
    "description": "Delivery log entries",
    "required": true
  },
  {
    "name": "deliveries[].id",
    "type": "string",
    "description": "Unique identifier for the delivery attempt.",
    "required": true
  },
  {
    "name": "deliveries[].destination_id",
    "type": "string",
    "description": "ID of the destination this delivery was sent to.",
    "required": true
  },
  {
    "name": "deliveries[].queue_id",
    "type": "string",
    "description": "ID of the queue.",
    "required": true
  },
  {
    "name": "deliveries[].message_id",
    "type": "string",
    "description": "ID of the message that was delivered.",
    "required": true
  },
  {
    "name": "deliveries[].payload",
    "type": "any",
    "description": "Payload delivered to the destination.",
    "required": false
  },
  {
    "name": "deliveries[].status",
    "type": "string",
    "description": "Status of the delivery attempt.",
    "required": true
  },
  {
    "name": "deliveries[].http_status_code",
    "type": "number | null",
    "description": "HTTP status code from the destination.",
    "required": false
  },
  {
    "name": "deliveries[].error",
    "type": "string | null",
    "description": "Error message if delivery failed.",
    "required": false
  },
  {
    "name": "deliveries[].duration_ms",
    "type": "number",
    "description": "Duration of the delivery attempt in milliseconds.",
    "required": false
  },
  {
    "name": "deliveries[].attempt_number",
    "type": "number",
    "description": "Which attempt number this was (1-based).",
    "required": false
  },
  {
    "name": "deliveries[].request_headers",
    "type": "object",
    "description": "Request headers sent to the destination.",
    "required": false
  },
  {
    "name": "deliveries[].response_headers",
    "type": "object",
    "description": "Response headers from the destination.",
    "required": false
  },
  {
    "name": "deliveries[].delivered_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the delivery was attempted.",
    "required": true
  },
  {
    "name": "deliveries[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the log entry was created.",
    "required": true
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/destinations/deliveries/order-processing/dst_abc123?limit=20" />

---

## Dead Letter Queue

### List DLQ Messages

List dead-letter queue messages.

<ApiEndpoint method="GET" path="/queue/dlq/list/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "limit",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Maximum messages to return"
  },
  {
    "name": "offset",
    "type": "number",
    "in": "query",
    "required": false,
    "description": "Offset for pagination"
  }
]} />

#### Response

Response containing DLQ messages and optional total.

| Status | Description |
|--------|-------------|
| 200 | DLQ messages returned |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Response Fields

<ResponseFields fields={[
  {
    "name": "messages",
    "type": "object[]",
    "description": "DLQ messages",
    "required": true
  },
  {
    "name": "messages[].id",
    "type": "string",
    "description": "Unique identifier for the DLQ entry.",
    "required": true
  },
  {
    "name": "messages[].queue_id",
    "type": "string",
    "description": "ID of the queue this message belongs to.",
    "required": true
  },
  {
    "name": "messages[].original_message_id",
    "type": "string",
    "description": "ID of the original message that failed (optional until backend includes it).",
    "required": false
  },
  {
    "name": "messages[].offset",
    "type": "number",
    "description": "Offset of the original message in the queue.",
    "required": true
  },
  {
    "name": "messages[].payload",
    "type": "any",
    "description": "The message payload (JSON object).",
    "required": true
  },
  {
    "name": "messages[].metadata",
    "type": "object | null",
    "description": "Optional metadata from the original message.",
    "required": false
  },
  {
    "name": "messages[].failure_reason",
    "type": "string | null",
    "description": "Reason why the message was moved to DLQ.",
    "required": false
  },
  {
    "name": "messages[].delivery_attempts",
    "type": "number",
    "description": "Number of delivery attempts before failure.",
    "required": true
  },
  {
    "name": "messages[].moved_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was moved to DLQ (optional until backend includes it).",
    "required": false
  },
  {
    "name": "messages[].original_published_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the original message was published (optional, falls back to published_at).",
    "required": false
  },
  {
    "name": "messages[].published_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the message was published (from base message).",
    "required": false
  },
  {
    "name": "messages[].created_at",
    "type": "string",
    "description": "ISO 8601 timestamp when the DLQ entry was created.",
    "required": true
  },
  {
    "name": "total",
    "type": "number",
    "description": "Optional total DLQ message count",
    "required": false
  }
]} />

#### Example

<ApiExample method="GET" path="/queue/dlq/list/order-processing?limit=20" />

---

### Replay DLQ Message

Replay a DLQ message back to the queue.

<ApiEndpoint method="POST" path="/queue/dlq/replay/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "DLQ message ID"
  }
]} />

#### Response

Replayed message object.

| Status | Description |
|--------|-------------|
| 200 | DLQ message replayed |
| 401 | Unauthorized |
| 404 | Queue or message not found |

#### Example

<ApiExample method="POST" path="/queue/dlq/replay/order-processing/msg_abc123" />

---

### Purge DLQ

Delete all messages in the dead-letter queue.

<ApiEndpoint method="DELETE" path="/queue/dlq/purge/{name}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | DLQ purged |
| 401 | Unauthorized |
| 404 | Queue not found |

#### Example

<ApiExample method="DELETE" path="/queue/dlq/purge/order-processing" />

---

### Delete DLQ Message

Delete a specific message from the dead-letter queue.

<ApiEndpoint method="DELETE" path="/queue/dlq/delete/{name}/{messageId}" />

#### Parameters

<ParamTable params={[
  {
    "name": "name",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "Queue name"
  },
  {
    "name": "messageId",
    "type": "string",
    "in": "path",
    "required": true,
    "description": "DLQ message ID"
  }
]} />

#### Response

Empty response on success.

| Status | Description |
|--------|-------------|
| 200 | DLQ message deleted |
| 401 | Unauthorized |
| 404 | Queue or message not found |

#### Example

<ApiExample method="DELETE" path="/queue/dlq/delete/order-processing/msg_abc123" />

---
